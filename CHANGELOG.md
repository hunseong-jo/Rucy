# 변경 이력

루시(my-agent)의 세션별 변경 기록. 상세·함정은 `knowledge/project_my_agent.md`,
기능 사용법은 `knowledge/manual_루시_사용설명서.md`(유일 원본)에 있습니다.

---

## 세션 77 — 2026-07-26: 폰 ↔ PC 양방향 Sync & 자가치유 · 일정 다중 통합 검색 · Termux:Boot & PWA 환경 구축

### 1. 폰 ↔ PC 양방향 Git 동기화 완벽 구축 및 자가치유(Self-Healing) 로직
- PC 및 스마트폰 부팅 시 자동 `git pull`을 수행하여 항상 최신 데이터 상태 유지.
- 대화 중 마무리/저장 관련 키워드("수고했어", "기억해줘", "저장해", "고생했어" 등) 입력 시 및 메모리 관련 도구(`remember`, `add_todo`, `add_event` 등) 실행 시 자율적으로 `git push`하여 변경 사항을 실시간 반영.
- 네트워크 단절이나 충돌 상황 발생 시 자가치유(Self-Healing) 백그라운드 동기화 모듈이 자동 개입하여 데이터 충돌 없이 동기화를 완성함.

### 2. 일정/메모 다중 통합 검색 버그 패치 및 날짜 파싱 보완
- 일정검색 요청 시 구글 캘린더(`list_events`) 외에도 장기 메모(`notes.md`), 지난 대화록(`search_memory`/`search_my_history`)을 동시 교차 다중 검색하도록 보완.
- "이번 주", "다음 주 목요일" 등 상대적 날짜 계산 오차 패치 및 연도 생략 날짜 파싱 로직 보완.

### 3. 폰 Termux:Boot 자동 실행 스크립트 (`setup_termux_boot.sh`) 구축
- 스마트폰 부팅 시 Termux:Boot 앱과 연동되어 루시 백그라운드 서버 및 양방향 Sync 데몬이 자동으로 실행되도록 `setup_termux_boot.sh` 환경 구축.

### 4. 대화 중 자동 Sync 및 오프라인 푸시 큐 (`pending_sync.json`) 구현
- 대화 맥락 감지 키워드 및 메모리 변경 도구 호출 후 양방향 Sync를 자동으로 발동.
- 오프라인 또는 네트워크 장애 상태일 때 푸시할 커밋을 `pending_sync.json` 오프라인 큐에 보관하고, 재연결 시 자동으로 재시도하도록 처리.

### 5. 웹 루시 PWA (`manifest.json`, `sw.js`) 스마트폰 앱 환경 구축
- 웹 루시 인터페이스에 PWA 표준인 `manifest.json`과 서비스 워커(`sw.js`)를 구현.
- 스마트폰 브라우저에서 '홈 화면에 추가' 또는 '앱 설치'를 통해 네이티브 앱처럼 아이콘으로 독립 실행 가능한 환경 제공.

---

## 세션 75 — 2026-07-23: 정기 점검에서 나온 수리 6종

**출발점:** 사용자가 "루시 점검해줘. 자세히 하나하나."

인프라 10종(`/점검`)·코드 46개 문법·도구 77종 명세↔구현·config 경로 23개는 전부 정상이었다.
문제는 **아무도 안 보는 자리**에서 나왔다.

### 1. 🔴 놀고 있는 두뇌 생사 확인 (`brainprobe.py` 신설 · doctor 11종째)

조용한 고장 경보(세션64)는 **불려본 두뇌만** 본다. 그런데 폴백 사슬은 위가 건강하면
아래를 안 부른다 — 5번째 두뇌가 폐기돼도 호출이 0회라 경보 문턱(4회)에 **영영 안 닿는다**.
**위가 건강할수록 더 안 보이고, 드러나는 순간은 예비가 가장 필요한 때다.**

실측으로 잡았다: **Groq이 `qwen/qwen3-32b`를 폐기해 404를 돌려주는데도 순위 5위에 그대로
남아 있었고**, 경보는 "만성 고장 없음"이라 답했다.

- 주 1회 새벽 05:45, **최근 거의 안 불린 두뇌에만** 짧은 인사를 보낸다(자주 쓰는 두뇌는
  계측이 이미 보고 있고 무료 한도도 아껴야 한다).
- ⚠️**죽음과 못 잰 것을 구분한다** — 404·폐기만 '죽음', 429·타임아웃은 판단 보류.
  못 잰 걸 죽었다고 적으면 멀쩡한 두뇌를 버린다(벤치·눈 시험이 같은 자리에서 배운 원칙).
- ⚠️**재기와 읽기를 나눴다** — 호출은 daily가, `/점검`은 적어둔 기록만 읽는다.
  안 그러면 루시를 켤 때마다 두뇌 열 개에 인사를 보내 시작이 그만큼 느려진다.
- 고치지는 않는다(모델 교체는 상의할 일). 사람이 교체하면 **경보가 스스로 사라진다**
  (config에 없는 라벨은 안 보고함 — 고친 문제를 계속 일러바치면 다음부터 아무도 안 본다).
- 덤: 임시 라벨(`점검 …`)이 `/상태` 계측판에 두뇌인 척 오르지 않게 걸렀는데,
  벤치가 남긴 기존 `시험 …` 10개 오염도 함께 걷혔다.

### 2. 🔴 `unity_health`가 5일째 조용히 안 돌던 것

이 일과만 `catch_up_hours`가 **2**(06:00~08:00)였다 — 나머지는 전부 18. 그런데 이 PC는
**09:20에 켜진다**. 창이 닫힌 뒤 부팅되니 영영 안 돌았다. 브리핑은 36시간 넘은 기록을
안 싣도록 돼 있어 **틀린 정보를 주진 않았지만, 아무 말도 안 했다**.

→ 18로 넓혔다. 겁내던 '브리핑 지연'은 기우였다 — 실측 **16.6초**. 브리핑 줄이 5일 만에
되살아났다(`''` → `'onlyuprat: 컴파일 문제 없음'`).

### 3. 🔴 죽은 Groq 자리 교체 + `tool_budget` 신설

후보를 넣어 보니 **3종 전부 즉시 413**. Groq 무료 티어는 분당 토큰 한도가 좁은데
도구 명세가 **약 15,700토큰**이라 질문 한 번에 죽는다.

- `tools.specs_for`에 **`tool_budget: "compact"`**를 새로 뒀다 → 약 **1,800토큰**(8.7배 감소).
  ⭐기존 `context`를 재활용하지 않은 이유: `context`는 '자리가 좁아서', `tool_budget`은
  '**한도**가 좁아서'다. 뭉뚱그리면 나중에 컨텍스트를 늘려도 413이 안 풀려서 헤맨다.
- ⭐**config에 적혀 있던 "gpt-oss-120b는 413이라 쓰지 말 것"이 틀렸다** — 모델 탓이 아니라
  명세 크기 탓이었다. 압축하니 **도구호출 4/4 정답·0.5~1.0초**로 후보 중 가장 깨끗했다
  (llama-3.3-70b는 옛 메모대로 지금도 4번 중 1번 `tool_use_failed`). NVIDIA에도 같은 모델이
  있지만 일부러 남긴 **공급자 이중화**다.

### 4. 🟠 `NVIDIA glm-5.2` 강등 (deep 해제 · reasoning 제거)

`deep:true`라 어려운 질문마다 앞으로 당겨졌는데, **추론 켜면 300초에도 못 끝냈고**(2회 재현)
꺼도 100~180초라 기본 timeout을 자주 넘겨 '끊김'으로 떨어졌다(최근 3일 평균 114초·실패율 50%
— 경보 문턱 60% **바로 아래**라 알람도 안 울렸다). 같은 deep 자리의 zai-glm(2.8초)·
gpt-oss-120b(0.5초)가 훨씬 빠르다. 지우지는 않았다 — '대화가 길어 Cerebras가 too_long으로
떨어질 때 받아주는' 원래 쓸모는 그대로라 뒤에서 느리게라도 답하는 예비로 남겼다.

### 5. 🟡 실수 노트의 **거짓 교훈** 삭제

> "장기 기억에 저장했다라고 답했으나 **실제로 지속 기억이 없으니** … 사용자에게 다시
> 입력받겠다고 안내할 것" (07-15)

루시는 `remember`/`notes.md`로 **지속 기억이 있다**. "장기 기억에 저장해줘"에 유사도 0.64로
붙는 것까지 확인했다 — **사장시킬 기능을 정확히 겨냥한 교훈**이었다. 삭제 후 재소환 0건
(원본 `memory/lessons.md.bak_s75`).

### 6. 🟡 멜로 첫 마디 79.7초 — 예열이 아니라 **페이지 축출**이었다

"부팅 시 예열을 걸자"가 애초의 처방이었는데 **서버는 이미 예열을 하고 있었다**(`_ready`를
켜기 전에 `synth("안녕하세요")`). 진짜 원인은 따로 있었다: 몇 시간 아무도 말을 안 시키면
윈도우가 그 프로세스를 디스크로 내보낸다 — **커밋 3.28GB인데 상주 35MB**. 첫 요청이 3GB를
도로 읽느라 79.7초가 걸렸고, timeout이 120이라 구글로 내려가지도 못했다.
**예열이 없어서가 아니라 예열해 둔 것이 쫓겨나서**라, 예열을 한 번 더 붙이는 건 답이 아니었다.

→ `tts.melo.first_timeout`(기본 20초) 신설. 첫 덩이만 짧게 기다리고 구글에 넘긴다.
⭐**넘기는 동안에도 서버 쪽 합성은 계속 돌아 페이지가 도로 올라오므로 다음 마디부터
로컬 목소리로 돌아온다** — 실측으로 20초에 끊긴 직후 다음 요청이 10.6초에 성공했다.
강제 실패 시험: 멜로 1.0초 실패 → 구글 0.4초 성공(총 1.4초). `first_timeout: 0`이면 옛 동작.

### 7. 👁 눈 보강 — 1개 → 판정단 3개 + 예비 2개 (사용자 요청)

"눈이 제미나이 하나는 너무 부족해, 3개 이상은 있어야 해."

**후보 13종을 2단계로 걸렀다.** 예선(정상 1장·문제 1장) → 통과한 것만 정식 채점
(eyecheck.run 그대로 재사용 — 채점 규칙을 베끼지 않으려고 임시 config에 후보를 실었다).

| 눈 | 등급 | 비고 |
|---|---|---|
| Gemini 3 Flash | 믿음 4/4 | 기존 |
| **NIM gemma-4-31b** | **믿음 4/4** | 신규 · 판정단 |
| **Gemini 3.1 Flash Lite** | **믿음 5/5** | 신규 · 이번 시험 유일한 만점 |
| NIM nemotron-3-nano-omni | 주의 (놓침 1) | 신규 · 예비(비구글) |
| NVIDIA nemotron-nano-VL | **강등** (놓침 2·오탐 1) | 판정권 회수 |

탈락 사유가 제각각이라 적어 둔다: `phi-3-vision`·`gemma-3-12b`는 카탈로그엔 있는데
**계정에 배포가 안 돼 404**("Function not found for account"), `gemini-2.5-flash`는
**신규 사용자 차단**, `llama-3.2-90b-vision`은 옛 메모대로 **지금도 타임아웃**,
`gemini-2.0-flash`·OpenRouter 무료는 **한도**. ⭐옛 메모의 "gemma는 404"는
gemma-3-12b 얘기였고 **gemma-4-31b는 멀쩡하다**.

- 🚫**`Groq qwen3.6-27b`는 눈에서 뺐다** — 그림 1장째 빈답, 2장째부터 한도로 30분 취침.
  ⭐**강등이 아니라 아예 답을 못 하는 것**이라 자리만 차지하고 앞의 눈이 실패했을 때
  호출 한 번을 헛되이 쓴다. 글 두뇌로는 그대로 쓴다.
- ⚠️**눈을 등록하기 전에 도구 호출을 반드시 확인할 것** — 도구를 못 쓰는 모델은 폴백 사슬에서
  '실패'가 아니라 **지어낸 답**을 내놓아 더 위험하다. 셋 다 확인했다(get_weather).

### 8. 👁 검수를 2개 합의제 → **3개 다수결**로 (사용자 결정)

세션64~74에는 '가장 믿는 눈 + 가장 못 믿는 눈'을 일부러 짝지어 이견을 끌어냈다. 취지는
옳았지만(비슷한 두 눈은 같이 틀려서 조용히 통과함), 그 자리의 nemotron이 놓침 2·오탐 1까지
나빠지자 **검수 2건 중 1건이 🙋 사람에게** 올라와(엇갈림 50% 실측) 자동 검수의 뜻이 옅어졌다.
→ **강등된 눈은 명단에 남기되 판정권을 회수**하고, 믿는 눈 3개의 다수결로 본다.
이견은 **믿는 눈들 사이에서** 갈릴 때 받는 편이 낫다.

- 과반이 모이면 그 판정(만장일치 / 다수결 2/3), **과반이 없으면(1:1, 1:1:1) 🙋 사람에게**.
- `_eye_pair` → `_eye_panel` + `_eye_tally`. `status.record_consensus`(짝 전용)는
  **`record_panel`(조합)** 으로 일반화하고 옛 이름은 입구로 남겼다.
- 강등된 눈은 **판정권 있는 눈이 전부 한도일 때만** 대타로 답한다(그때는 '혼자 본 판정' 표시).
- config: `vision.eyes`(기본 3). `two_eyes:false`면 예전처럼 1개.
- 검증: 표 계산 7가지 경우를 픽스처로(한도 0) + **실물 3개 호출로 두 경우 만장일치 정답**(8~9초).

⚠️**남은 위험 — 판정단 3개가 전부 구글 계열이다**(Gemini 2 + Gemma). 셋이 같은 약점을
공유하면 3:0으로 사이좋게 틀려 조용히 통과한다 — 합의제가 막으려던 바로 그 구멍이다.
비구글 눈을 찾아 13종을 뒤졌지만 믿음 등급이 하나도 안 나왔다(omni는 주의에 그침).
지금은 omni를 **예비**로 뒤에 세워 두는 선에서 막았다(실측상 한도가 가장 자주 마르는 쪽이
Gemini라, 계열·공급자가 다른 눈이 뒤에 있어야 눈이 통째로 멎지 않는다).

### 🔎 아직 안 고친 발견 (사람이 정할 일)

- **자동 후순위(rank)가 실제 대화에는 안 걸린다.** `call_model`은 `order`가 None일 때만
  `status.rank`를 쓰는데, 대화 경로는 `pick_order`가 **항상** 순서를 넘긴다(agent.py 879 → 701).
  그래서 `/상태`가 "↓ nemotron-3-super를 뒤로 미뤘습니다"라고 말해도 **대화에서는 안 밀린다** —
  현황판이 사실과 다른 말을 하는 상태다. 고치려면 `pick_order`가 rank를 태우면 되지만
  라우팅 동작이 바뀌므로 승인이 필요하다.
- **C: 여유 18.8GB** — 경고 문턱 15GB에 근접.
- **판정단이 전부 구글 계열** — 위 8번 끝의 ⚠️ 참조. 믿을 만한 비구글 눈이 생기면 바꿀 것.

---

## 세션 70 — 2026-07-22: 어려운 질문을 '깊게' 생각하는 4중 장치

**출발점:** 사용자가 "루시 사고 능력을 올려주고 싶어."

**실물 진단:** 어려운 질문 파이프라인엔 이미 계획(PLAN_HINT)·과거 실수 회상·
자가 감수·`/검증`이 있었지만, **두 구멍**이 있었다 — ①11개 두뇌 중 아무에게도
추론 모드(reasoning)가 안 켜져 있어 **추론 특화 두뇌가 얕은 모드로** 답했고,
②`deep=true`가 nemotron 1개뿐이라 "어려움→깊은 두뇌 먼저" 라우팅이 거의 안 먹었다.

**추가한 4중 장치(전부 config `deliberate`로 끔·쉬운 질문/보는 턴엔 안 켜짐):**

1. **추론 모드** — 두뇌별 `reasoning` 필드 신설 + `call_model(deep_think=)`로 어려운
   질문에서만 주입. gpt-oss-120b·glm-5.2·mistral-medium-3.5 = `{"reasoning_effort":"high"}`,
   nemotron-3-super = `{"system":"detailed thinking on"}`. **⭐400 안전망**: 추론 인자를
   거부하는 두뇌는 **버리지 않고** 인자만 빼서 재시도(`_no_reason`) → 답을 못 받는 일이 없다.
   ⚠️실측: high는 느리다(사소한 질문에 glm 114초·mistral 79초, gpt-oss 0.8초) →
   깊은 두뇌를 **빠른 것부터** 정렬(Cerebras→gpt-oss→…)해 느린 둘은 뒤 폴백으로만.
2. **deep 손질** — 강한 추론 두뇌 5개에 `deep=true`(라우팅이 실제로 발동).
3. **독립 감수** — 감수를 **답한 두뇌와 다른 두뇌**가 하도록(자기 답 자기검토 방지).
   추가 호출 없음(감수 1회 그대로). `deliberate.review_independent`.
4. **분해 지시** — 여러 갈래 섞인 큰 질문에 "하위로 쪼개 각각 풀고 종합" 한 줄 추가
   (`is_very_hard`·`DECOMPOSE_HINT`, 추가 호출 없음).

**검증:** gpt-oss·glm-5.2·mistral 모두 `reasoning_effort` 수용(400 없음) 실측 확인,
respond() E2E에서 라우팅 `+ 깊게 생각` + 계획 + 독립 감수(실제로 답을 고침)까지 동작 확인.
백업 `config.json.bak_s70`·`agent.py.bak_s70`. 사용설명서·지식색인 갱신.

---

## 세션 68 — 2026-07-21: 사진+제작 요청 손바닥 뒤집기·엉뚱한 파일명 버그 수리

**출발점:** 사용자가 참조 사진을 보내며 "블렌더로 만들어줘"라고 했더니 루시가
**"그런 기능 없다"**고 부정 → 사진 없이 "너 그 기능 있잖아"라고 다시 말하니
**"할게요"로 손바닥 뒤집기** → 정작 만들 땐 파일명을 **엉뚱하게 '샐러드팜~'**로 붙임.

**원인은 버그 2개가 커플링된 것이었습니다:**
- **① 사진 턴은 도구가 통째로 꺼진다**(`agent.py`: `use_tools=not images`). 이걸 막는
  `vision.wants_edit` 예외가 `화풍|바꿔|그려줘`만 알고 **`만들어·블렌더·모델링·3D`를 몰라서**,
  "사진 보고 블렌더로 만들어줘"가 '그냥 보는 턴'으로 빠졌다 → 도구 없음 → "기능 없다".
  다음 턴은 사진이 없어 도구가 켜지니 "할게요"로 뒤집힘.
- **② 그때는 사진이 이미 문맥에서 버려진 뒤**(이미지는 한 턴만 삶)라 루시가 사진을 못 본 채
  만들면서 파일명을 지어냈고, 주입된 기억이 온통 **샐러드팜**이라 그걸 집었다(문맥 오염 환각).

**수리(2단계 — 1차는 방향이 반쯤 틀려 2차에서 바로잡음):**
- **⭐핵심 통찰:** "사진 보고 3D로 만들어줘"는 **도구가 아니라 눈이 필요한 일**이다 —
  `build`는 사진을 인자로 못 먹으므로 루시가 **눈으로 형태를 읽어** 치수로 옮겨야 한다.
  이건 `restyle`(도구가 이미지 파일을 직접 재료로 먹는 편집)과 **정반대**다.
  1차 수리는 이 둘을 한 바구니에 넣어 "도구 켜고 눈 끄기"로 처리했다가, 루시가
  **"사진을 직접 볼 수 없다"**고 답하는 걸 사용자가 발견 → 2차에서 갈라놓음.
- **`vision.py` 갈래 나눔** — `wants_edit`(restyle류)은 `화풍·스타일·바꿔·그려줘`만 잡아
  **도구 켜짐**. 새 `wants_make`(3D 제작류: `만들어·제작·모델링·조립·블렌더·3d모델·입체로`)는
  **보는 턴으로 두어 눈이 켜지게** 한다(도구는 이 턴엔 꺼짐). 11케이스 실측 통과.
- **`agent.py` `MODEL_FROM_REF` 힌트 신설** — 사진+제작 요청이면 보는 턴에 이 안내를 심는다:
  "너는 3D 도구를 실제로 가지고 있으니 '기능 없다'고 하지 마라. 그림을 직접 보고 ①무엇인지
  ②형태·비율 ③특징을 설명한 뒤, 어떤 치수 그레이박스로 만들지 **네가 먼저 제안**하라
  (사용자는 고쳐주기만 하게). 실제 조립은 다음 턴에 도구로." → 루시가 **사진을 눈으로 보고**
  형태를 읽어 제안하게 됨(사용자에게 맨입으로 치수 부르라 하지 않음).
- **`blender_3d` 도구 설명 강화(1·2차 공통)** — `subject`·`path`는 지금 사용자가 말한
  대상에서만 따오고 **기억 속 다른 프로젝트명(샐러드팜 등)을 파일명으로 쓰지 말 것**,
  불분명하면 되물을 것. **사진→3D 자동 재현은 없다** — 한계를 먼저 정직하게 말한다.

⚠️ **미해결로 남긴 것:** 사진을 '보면서 동시에 도구'를 쓰는 건 여전히 불가(눈 달린 모델이
도구 명세와 함께 오면 거절하는 제약 때문). 그래서 제작은 두 턴이 된다 — 1턴: 보고 제안,
2턴: 도구로 조립. 하지만 이제 **거짓 부정도, 엉뚱한 파일명도, 사진 무시도 없다**.
근본 해결은 눈+도구 동시 지원 모델이 필요.

---

## 세션 67 — 2026-07-20: 블렌더 조형 3종 — 기능 조회·표면 요철·닮음검수

**출발점:** 사용자가 루시에게 토마호크 스테이크 모델링을 시켰더니 **망치**가 나왔고
(실린더 1개 + 똑같은 큐브 40개, 0.24m·490tris — 요청은 6m급),
"뭐가 부족하냐"고 묻자 루시가 **스컬프·베이킹·UV 언랩·아틀라스 4종을 추가해 달라**고 답했습니다.

**실측 결과 루시의 진단은 대부분 틀렸습니다** — 4종 중 3종(`bake`·`uv`·`uv_atlas`)은
이미 있는 기능이었습니다. 도구 설명이 4,000자를 넘어 **자기가 가진 기능을 회상하지 못한 것**입니다.
그리고 진짜 실패 원인은 도구가 아니라 **형태 설계**였습니다(같은 도구셋으로 만든
steak_v4는 32,000tris·6.6m로 멀쩡함).

- **`action=help` 신설** — 기능 목록을 **회상 말고 조회**하게. 블렌더를 켜지 않아 즉시·공짜.
  유일 원본은 `blender3d.py`의 `CATALOG`, **못 하는 것**(`CANNOT`)도 같이 실어 양방향 오답을 막음.
  도구 설명 맨 앞에 "'그 기능은 없다'고 말하기 전에 반드시 help를 먼저 부른다"를 못박음.
- **`sculpt_displace` 신설** — 브러시 스컬프의 헤드리스 대체(손이 없어 브러시는 불가능).
  면을 나눈 뒤 절차적 텍스처로 표면을 밀어 유기적 요철: `bumpy`(빵)·`wrinkle`(채소)·
  `groove`(결·골)·`cell`(참깨·치즈)·`rough`. 서브디비전은 `SIMPLE`(Catmull은 실루엣이 오므라듦),
  요철은 자동 스무스 셰이딩. **형태는 안 만들고 표면만** — build로 덩어리를 잡은 뒤 쓰는 마감 도구.
  면 폭주 방지 상한(`max_tris` 기본 30만)을 실행 **전에** 검사.
- **🪞 닮음검수 신설** — 기존 눈검수가 그 망치를 통과시킨 이유는 **파손만 묻기 때문**
  ("부품이 뭉치거나 소실됐는가?" → 부품은 다 제자리라 '정상'). build·assemble 뒤
  **"이 물체는 무엇으로 보입니까?"를 열린 질문으로 먼저 묻고** 의도(`subject`)와 대조.
  ⭐정답을 먼저 알려주면 눈이 그냥 동의해 버려서(유도신문) 이름을 먼저 말하게 함.
  `subject` 미지정 시 파일명에서 추출(`tomahawk_highpoly`→`tomahawk`).

**실측 검증:** 망치 렌더 → 눈 2개 일치 "문제: 받침대나 스탠드처럼 보입니다" ✅ /
버거 → "정상, 햄버거입니다" ✅ / 스테이크 → 눈 엇갈려 🙋사람에게(합의제 정상 동작).

⚠️**E2E가 잡은 버그 2건**(단위 실행만으론 안 보였음): `_WHAT`에 항목 누락으로 KeyError,
그리고 **`pattern`·`subdiv`가 러너까지 전달되지 않아 항상 기본값으로 실행**(안전장치도 무력화)
— 수술마다 kw를 손으로 넘기는 구조라 새 action은 그 분기를 반드시 추가해야 함.

---

## 세션 65 — 2026-07-19: 눈 신뢰도 올리기 — 기계 관문 + 시험지 수리 + 한도 재시도

**요약:** "눈 신뢰도를 어떻게 올리나"에서 출발해 실측해 보니 원인이 셋 다 달랐고,
그중 **하나는 눈이 아니라 시험지 잘못**이었습니다.

- **① 🔧 기계 판정 관문(vision.machine_verdict) — 눈에게 안 묻고 끝내는 것들**: 정확한
  마젠타 5%↑·단색 99.5%↑·'어둡고 동시에 밋밋함'은 픽셀만 세면 확실하므로 눈 두뇌를 부르기
  전에 확정. nemotron이 **온통 마젠타를 '정상'이라 통과시킨** 사고 경로를 구조적으로 제거.
  블렌더 쪽엔 이 방어막이 **아예 없었고**(유니티만 일부 있었음), 마젠타는 유니티에서도
  메모만 달고 눈에게 물어보던 상태였음. 계산은 `vision.pixel_stats` 하나로 통일
  (`unity._pixel_stats`는 위임).
  - ⚠️**실측 오탐 1건을 잡아 임계값 수리**: onlyuprat 배관 스테이지(평균밝기 4.5)가
    '렌더 실패'로 걸렸으나 파이프·발판·아이템이 멀쩡한 **정상 화면**이었음. '어둡다'≠'검은
    화면' — 진짜 실패는 밋밋함(std 0.00 vs 정상 어두운 씬 5.62)이라 **둘 다** 만족할 때만
    판정. 실물 렌더 38장 재검사로 오탐 0건 확인.
- **② 시험지 수리(eyecheck)**: 🚨**'바닥에 잠긴 상자' 문항이 두 세대에 걸쳐 등급표를
  망가뜨리고 있었음** — 반투명 바닥 아래가 **광택 바닥의 반사**로 읽혀(3D에서 반사는 정상)
  눈들이 '정상'이라 답했고, 놓침 2배 벌점 탓에 Gemini가 부당하게 '주의'·nemotron이 '강등'.
  바닥 격자선을 상자 위로 지나가게 다시 그려도 소용없었음(Groq 2회 연속 '정상' — 격자를
  보면서도). ⭐**결론: 바닥 관통은 눈의 일이 아니라 기계의 일**(최저 z 측정) → 문항 제거.
  검은 화면·단색 마젠타는 기계 담당이 됐으므로 시험에서도 빼고, 대신 **'일부만 분홍'**
  (기계가 확답 못 하는 구간=눈이 실제로 필요한 자리)과 **'어둡지만 정상인 화면'**(오탐 쪽
  측정, 위 실측 오탐에서 나온 문항)을 추가. 6장(정상2/문제4) → 5장(정상3/문제2).
- **③ 한도 재시도 + 사유 기록**: 무료 눈은 이미지 2~3장이면 분당 한도 → **30분 쿨다운**에
  들어가 남은 문항이 통째로 날아감(재현 확인). 그래서 Groq이 계속 '측정 불가'였고 2개 눈
  합의제의 한 자리가 비어 있었음. 이제 한도로 못 물어본 문항은 미뤄뒀다 쿨다운이 풀리면
  다시 물음(새벽 정기 시험만 최대 45분 대기 — `daily.eye_trust.max_wait_sec`. 대화 중
  도구는 안 기다림). `_ask`가 모든 실패를 삼키던 것을 고쳐 **한도/빈답/형식/예외를 구분**해
  기록(예전엔 원인을 알 수 없어 재현 실험을 따로 해야 했음). `agent.resting_until()` 추가.
- **덤 실측**: nemotron은 같은 그림에 3회 물으면 정상/문제/정상으로 **답이 오락가락**하고
  출력에 잡음("유리질")도 섞임 — 강등이 타당하며, 사용자님 설계대로 '이견 내는 자리'로 유지.
- 파일: `vision.py`, `eyecheck.py`, `tools.py`, `unity.py`, `agent.py`, `daily.py`,
  `knowledge/manual_루시_사용설명서.md`.

### 같은 세션 2부 — 두뇌 재배치 + NVIDIA 추가 (10 → 11단)

"여전히 버벅이고 무료 한도가 잦다"에서 출발. **계측(metrics.json) 4일치를 먼저 읽어** 원인을
짚었습니다: 한도 실패 20건 중 **18건이 Gemini(9)+Groq(9)**, 버벅임도 Gemini(평균 9.0초·최대
26.8초, 다른 두뇌의 2~4배)인데 **하필 호출 1위(49회)**. 반면 NVIDIA 3종은 같은 기간 한도 실패 0건.
두뇌가 모자란 게 아니라 **한도가 빠듯한 자리가 혹사당하는 구조**였습니다.

- **재배치**: `deep`을 Gemini → **NVIDIA nemotron-3-super**로. 실측 검증 1.0~2.7초(7배 빠름),
  추론 정답, 한국어 정상. Gemini는 **눈 전담**으로 남김.
  - ⚠️Cerebras를 deep으로 하려다 **철회** — `context: 8192` 상한에 `too_long` 실패가 실제로
    찍혀 있어, 대화가 긴 어려운 질문에는 부적합.
- **NVIDIA glm-5.2 추가(11단째)**: 카탈로그 119종 조회 → 후보 5종을 기존 `/벤치` 과제로 실측.
  **glm-5.2 3/3·평균 9.2초로 1위**(deepseek-v4-pro 3/3·24.6초, qwen3.5-397b 3/3·**162.9초**,
  qwen3-next-80b 1/3, kimi-k2.6은 카탈로그에 있으나 **404**). ⭐Cerebras의 zai-glm-4.7과 같은
  GLM 계열 상위 버전인데 **8192 컨텍스트 상한이 없다** — Cerebras가 too_long으로 떨어질 때 받는 자리.
- 🚨**bench.apply의 deep 자동 선정을 폐지(사용자 결정: 수동 고정)**: 옛 규칙은 `max(pool,
  key=secs)` = **'가장 느린 정확한 모델'**에게 deep을 줬다. "느리지만 정확한 쪽"이라는 전제였지만
  **'느림'은 숙고의 증거가 아니었다** — Gemini가 느렸던 건 인프라 지연과 한도 스로틀링 탓이고,
  그 규칙 때문에 버벅임의 주범이 deep을 차지하고 있었다. ⭐더 근본적으로 **벤치의 3과제는 '어려운
  질문 능력'을 아예 재지 않는다** — 재지도 않은 것을 결과로 추론하면 안 된다. 이제 deep은 사람이
  config에 적고 벤치는 순서만 다시 쓴다(임시 사본으로 보존 검증 완료).
- ⚠️집계 실수 자백: 후보 평균 속도를 처음에 **3배 낮게** 보고할 뻔했다 — `bench`의 결과 dict가
  이미 평균(`total_secs/tried`)인데 내 스크립트가 한 번 더 나눴다. 표를 내기 전에 원자료와 대조해 잡음.
- 파일: `config.json`(+백업 `.bak_s65`), `bench.py`.

### 같은 세션 3부 — 문서 능력 확장 (한글 신설 + PPT 그림·표 + 엑셀 서식·수식·다중시트)

"파워포인트·한글·엑셀 능력을 올리자". 조사부터 하니 **한글은 읽을 줄만 알고 쓸 줄은 몰랐고**,
PPT는 글상자뿐, 엑셀은 값만 들어가는 상태였습니다. `docs.py`의 '라이브러리 없이 zip+XML'
원칙을 그대로 지켜 확장했습니다(한글이 안 깔린 PC에서도 .hwpx가 만들어집니다).

- **한글(.hwpx) 쓰기 신설**: OWPML을 손으로 씁니다. ⚠️오피스와 결정적 차이 = 본문이 글자모양·
  문단모양을 **번호로만** 가리키고 실체는 header.xml에 있어, 번호 하나가 어긋나면 한글이 파일을
  통째로 거부합니다. ⚠️mimetype은 zip 첫 항목 + **무압축**(OCF 규약). 참조 구조는 규격서 추측이
  아니라 **한글 COM으로 실제 파일을 뽑아 뜯어봐서** 알아냈습니다.
  - 검증 3중: 루시가 씀 → **한글이 열어** 텍스트로 되저장 → 루시가 다시 읽음. 세 결과 완전 일치.
- **PPT 그림·표**: `![](경로)` 또는 `그림: 경로`로 이미지 삽입(media 부품+rels+Content_Types
  Default 등록, 원본 비율 유지 — PIL 없으면 PNG/JPEG 머리글을 직접 읽어 크기 파악).
  표는 **진짜 표**(graphicFrame+a:tbl)로. 예전 주석의 "표는 부품이 많아 위험"은 사실이 아니었고
  (표는 별도 부품이 없음) 슬라이드 XML 안에서 끝납니다. 그림이 있으면 글 왼쪽·그림 오른쪽 배치.
  - 🐞**표 높이를 남는 공간에 늘렸다가 세 줄짜리 표가 화면을 가득 채움** → 파워포인트로 렌더해
    **눈으로 보고** 잡음. 표는 내용만큼만 차지하게 수정.
  - 검증: PowerPoint COM으로 열어 개체 종류 확인(그림=Type13·표=Type19) + 슬라이드 PNG 렌더 눈검수.
- **엑셀 서식·수식·다중시트**: styles.xml 신설(머리행 굵게+배경+테두리, 천 단위 `#,##0`),
  열 너비 자동(한글 폭 1.6배 계산), 머리행 틀 고정, `=SUM(...)` 수식, `# 시트이름`으로 시트 분할.
  - 🐞**여러 시트가 한 표로 뭉개지던 버그**: `[(이름, 표)]`인지 판별하며 `sheets[0][0]`(=이름,
    문자열)이 리스트인지 검사했음 → 짝의 **두 번째**를 봐야 맞음.
  - 검증: Excel COM으로 열어 시트 2장·수식 실제 계산(=SUM→67902)·머리행 굵기·열 너비·
    표시형식(24,500) 확인.
- 파일: `docs.py`, `tools.py`(write_document 설명·docstring), `knowledge/manual_루시_사용설명서.md`.

---

## 세션 63 7부 — 2026-07-18: 두뇌 손질 — 눈 3단 + 비전 다이어트 (두뇌 9 → 10단)

**요약:** 판단력 작업으로 눈 호출이 급증해 무료 눈 2개가 실제로 말랐던 것(오후 실측) 수리.

- **눈 두뇌 3단**: NIM 카탈로그 119종 실측 조회→비전 후보 눈시험(마젠타 그림) — **nvidia/nemotron-nano-12b-v2-vl 합격**(한국어로 "마젠타색 정사각형" 정확, 멀티이미지도 됨)만 등록. 탈락 실측: gemma-3(계정 404)·llama-3.2-11b-vision(배경색·배치 오답=거짓 판정 위험)·90b/maverick(콜드스타트 타임아웃 180s). 눈 순서 Gemini→Groq→NIM.
- **비전 호출 다이어트(_eye_look_many)**: unity_shot이 씬마다 눈을 부르던 것을 **3장씩 한 턴에 묶어** 장별 판정(씬 7개=호출 7→3회). ⚠️실측 함정 2: **Groq 눈은 이미지 3장 상한**(4장=400) · 모델마다 답 형식 제멋대로("1: Intro, 정상 = …")→엄격 파싱 대신 이름+정상/문제 낱말 관용 파서. 판정 누락 씬은 "판정 못 받음" 정직 표기(침묵=정상 오해 방지).
- E2E: 4씬→2호출, NIM 눈이 실전 인계(Gemini·Groq 동시 한도 중), Intro 타이틀 "My little Salad farm"까지 읽음. ③계측 기반 자동 후순위는 다음 세션 후보로.
- 파일: `config.json`(+백업 .bak_s63), `tools.py`.

---

## 세션 63 6부 — 2026-07-18: 판단력 심화 5종 (도구 74 → 75종)

**요약:** "판단력 더 올릴 게 있을까?" → 5방향 제안, "1번부터 순서대로" 승인.

1. **unity_fbx_check(신규)** — 블렌더 납품 FBX(+옆 텍스처)를 검증 프로젝트(config unity.verify_project, 기본 onlyuprat)에 **실제 임포트**: 재질별 mainTexture 바인딩 실측(NULL=갈색 신호)+렌더+마젠타+눈검수, 흔적 청소. 실물 E2E: bar.fbx — 데칼 재질에 bar_decal_baked 물림 실측·로고 렌더 확인·_LucyVerify 잔재 0.
2. **전/후 비교 눈검수** — 형태 바꾸는 수술 14종은 원본 렌더(쿼터 앵글)+후 미리보기를 좌우 합성(`_전후.png`)해 "의도한 변화: {수술 설명}"과 비교 판정. ⭐E2E가 잡은 것 3건: 불리언 도구 소멸을 파손으로 오판(→_COMPARE_NOTES로 정상 고지)·수술 미리보기 태양이 회전 없는 수직 낙사라 옆면 새까맘(→렌더 액션과 회전 통일)·**안 겹치는 불리언(변화 없음)을 눈이 정확히 잡아냄**(테스트 스펙 실수까지 검출). 재검증: 벽+구멍 정상 판정, Gemini 한도 중 Groq 비전 폴백 실전 확인.
3. **unity_shot play=true(플레이모드)** — PlayMode 테스트(임시 어셈블리 심고 지움)로 씬을 **실제 굴리고 2초 안정 후** 캡처 — 런타임 UI·스폰이 보임. 런타임 예외 수 요약 표기, InitTestScene 자동 제외. ⭐E2E가 잡은 함정 2+발견 1: ①**Burst가 임시 테스트 어셈블리 잡 재컴파일에 걸려 20분 매달림**(Mono.Cecil resolve 실패) → `-burst-disable-compilation`(run_batch burst 인자) ②**비정상 종료(컴파일 에러 exit1·타임아웃 킬)는 UnityLockfile을 안 지움** → run_batch가 자기 잠금을 스스로 청소(_sweep_lock — 프리플라이트 통과 후 부팅이라 에디터 잠금일 수 없음) ③**saladfarm의 MCP 패키지(com.ivanmurzak.unity.mcp)가 14:20 갱신 후 배치 컴파일 깨짐**(CS0115) — 루시 문제 아님·사용자 보고 사항. 실물 E2E는 onlyuprat에서: 플레이모드 3씬 캡처(치즈 발광 등 런타임 상태 실증), 눈 두뇌 한도 소진 시 "못 함" 정직 폴백 확인. 우리 템플릿 폐기 API(FindObjectsSortMode) 4곳도 정리.
4. **애니 프레임 눈검수** — anim_preview가 GIF에서 4프레임 띠(`_프레임띠.png`)를 뽑아 관통·바닥 꺼짐·정지만 판정(자연스러움은 사람 몫 명시).
5. **샷 리그레션 diff** — unity_shot이 같은 씬 직전 샷과 픽셀 비교(>5%면 📏 경고, 에디트/플레이 분리·과거 폴더만). 같은 씬 재촬영 0.0% 실측.
- 파일: `unity.py`(fbx_verify·play_shot·prev_shot_diff), `tools.py`, `blender_runner.py`(미리보기 태양 회전), 매뉴얼.

---

## 세션 63 5부 — 2026-07-18: 🎓 작품 지적 학습 (판단력 3층 = 로드맵 완성)

**요약:** "루시 작업하자" — 남은 3층. "천이 너무 뻣뻣해" 같은 **품질 지적**(명시적 "틀렸어"가 아닌)을 학습 신호로.

- `lessons.is_craft_complaint(text, last_answer)` — 2중 게이트: 품질 불만 표현(어색·뻣뻣·과해·겹쳐·너무 크/작…) **+ 직전 답이 작품 보고**(CRAFT_MARKERS: "사본에 반영했습니다"·"씬 스크린샷" 등)일 때만. 단위 8/8("오늘 날씨 이상해"·"내가 잘못 눌렀어" 등 오탐 차단).
- `CRAFT_PROMPT`(learn kind="craft") — 교훈을 **"다음에 그 도구를 부를 때 인자를 어떻게 바꿀지"**로 뽑고 **도구 이름(영문) 포함을 강제**(회상 열쇠). 실물 추출: "physics_bake cloth는 frames를 45에서 더 늘려…" ✅.
- agent.respond 훅 확장: is_correction **또는** is_craft_complaint → craft면 kind="craft"로 학습.
- ⭐**회상 함정 실측**: 지시문 어법 교훈 vs 요청 어법 질문은 bge-m3 임베딩 0.47~0.51 — 기준선 0.55에 **영원히 안 걸림**(무관 질문과는 0.38로 분리됨) → **도구 이름 든 교훈만 전용 기준선 0.45**(config lessons.craft_min_score, 낱말 폴백은 그대로). E2E 5/5(천 요청 3종 회상·날씨/일정 조용).
- 이로써 판단력 로드맵 완성: **1층 👁 눈검수(만들고 스스로 봄) · 2층 📏 예산 대조(수치에 자 대기) · 3층 🎓 지적 학습(지적이 다음 기본값이 됨)**.
- 파일: `lessons.py`, `agent.py`, 매뉴얼.

---

## 세션 63 4부 — 2026-07-18: 유니티 판단력 (unity_shot + 빌드 예산, 도구 73 → 74종)

**요약:** "유니티도 판단력 올리고 싶어" — 블렌더와 같은 2층을 유니티에.

- **unity_shot(신규)** — 빌드 씬을 배치모드로 **실제 렌더**해 PNG(720×1280·landscape·scene 필터). 세션58 함정을 코드에 박음: ①ShaderUtil.allowAsyncCompilation=false+예열 3회(검정 플레이스홀더 방지) ②run_batch에 graphics 인자 신설(-nographics 빼야 렌더됨) ③**Screen Space Overlay UI는 렌더 동안만 카메라에 붙여** 찍음(씬 저장 안 하므로 무해). 임시 LucyShotTemp.cs 심고 지움.
- 샷 3중 판정: **📏 마젠타 픽셀 스캔**(0.5%↑=재질 실종, 합성 이미지로 9.5% 실측 검증) → **단색 화면 기계 판별**(최빈색 98%↑ — 눈 두뇌 호출 안 함) → 나머지만 **👁 눈검수**(검은 화면·UI 겹침·잘림).
- **📏 빌드 예산** — unity_build_report가 크기를 config unity.budgets.apk_mb(기본 150)와 대조, 초과 시 처방 한 줄. `knowledge/unity_모바일_기준표.md` 신설(빌드·씬 린트·텍스처·모델 기준의 근거·예외, search_knowledge 대상).
- ⭐실물 E2E가 가르쳐준 것: saladfarm 7씬 중 4씬은 UI까지 선명(Hatchery에 **확인 팝업이 켜진 채 저장된 것**까지 보임), 3씬(Shop 등)은 단색 — **깨진 게 아니라 UI를 런타임에 만드는 씬**. 처음엔 눈이 "문제: 비어 있음"으로 오탐 → 단색은 픽셀로 기계 판별해 ℹ정보로 강등(블렌더 천 오탐과 같은 교훈: 정상 상태를 문구에 명시).
- 파일: `unity.py`(run_batch graphics·_SHOT_CS·scene_shot·_pixel_stats·build_report 📏), `tools.py`(_eye_look 공용화·unity_shot·게이트), 기준표, 매뉴얼.

---

## 세션 63 3부 — 2026-07-18: 📏 모바일 예산 자동 대조 (판단력 2층)

**요약:** "2번도 해줘" — 수치를 기준과 대조하는 기계적 판단. 기준표 문서 + 도구 자동 경고.

- `knowledge/blender_모바일_기준표.md` 신설(유일 원본): 소품 5,000tri·캐릭터 30,000tri·재질 8종·본 75개 + 근거·예외·처방. search_knowledge 조향에 "3D 에셋 예산 질문엔 기준표" 추가.
- `tools._BUDGETS` + config `blender.budgets` 덮어쓰기. 걸린 곳: **info**(runner가 total_tris·bones 신설 — 삼각형 정확 계수, **뼈대 있으면 캐릭터 기준 자동 판별**)·**check**·**폴리 바꾸는 수술 6종**(bevel·solidify·decimate·boolean·mirror·curve_path — 면×2 추정 "약" 표기).
- ⭐E2E가 잡은 것: check의 `tris`는 총 삼각형이 아니라 **'삼각형 면의 개수'**(쿼드 파일=0) — 예산 대조에 썼다간 영원히 침묵할 뻔. 면×2 추정으로 교체.
- 실물 E2E: 기본 예산=침묵✅·예산 축소 config=경고✅·뼈대 파일=캐릭터 기준+본 경고✅·bevel 결과 📏 줄✅·check 추정 줄✅.
- 파일: `tools.py`, `blender_runner.py`(info tris·bones), `blender3d.py`(요약에 삼각형·본), 기준표, 매뉴얼.

---

## 세션 63 2부 — 2026-07-18: 👁 블렌더 수술 자가검수 (판단력 1층)

**요약:** "판단력을 올리려면?" → 1순위 '비전 폐루프' 구현. 수술이 미리보기를 찍기만 하고 정작 루시가 안 보던 것을, **눈 달린 두뇌(vision.capable)에게 보여 1차 검수**시키고 결과 줄에 "👁 자가검수: 정상/문제: …"를 덧붙임.

- `tools._blender_eye(action, preview, point)` — vision.user_message로 이미지 한 턴 + `agent.call_model(use_tools=False, order=눈 두뇌만)`. 세션42 비전 규칙(이미지 한 턴만·도구 끔·눈 없는 두뇌 금지) 그대로 준수.
- 수술별 체크포인트(`_EYE_POINTS`) — "미적 평가 금지, 이것만 판정" + '정상/문제:' 형식 강제. 명백한 실패(공중 부양·한 점 뭉침·소실·뒤집힘)만 잡는 층 — 미적 판단은 여전히 사람.
- 걸린 곳: 수술 전체(미리보기 있는 것)·build·assemble. 실패해도 수술 결과 불가침(못 보면 "생략/못 함" 한 줄). config `blender.eye_check=false`로 끔.
- ⭐E2E가 잡은 오탐 2건과 수리: ①**천 드리움의 늘어진 자락을 '공중 부양'으로 오판** → cloth 전용 문구("자락 늘어짐은 정상, 통째 부양·완전 낙하만 문제") ②**배치류를 낮은 앵글로 찍으니 앞뒤 물체가 겹쳐 보여 '겹침' 오판**(수치상 3D 겹침 없음 실측) → **align·array·scatter 미리보기를 부감(카메라 z=dist×0.85)으로** + "완전히 포개졌을 때만 문제" 문구.
- 실물 E2E: 천 드리움=정상✅·공중 상자(굽기 전)=문제✅(진짜 문제를 진짜로 잡음)·grid 정렬=부감 후 정상✅·도구 관통(👁 줄 포함)✅·눈 두뇌 없음=정직 생략✅·끔 스위치✅.
- 파일: `tools.py`, `blender_runner.py`(부감 카메라), 매뉴얼.

---

## 세션 63 — 2026-07-18: 블렌더 10종 (41 → 51동작)

**요약:** "블렌더 루시 도구 10개 추가시켜줘" — 백로그 차점 3종(pose_apply·physics_bake·purge) + 실전 빈틈 7종. 도구 수는 그대로(73종) — blender_3d 동작 확장.

- **bevel** — 모서리 챔퍼(width·segments·angle 제한=평면 불가침). **solidify** — 안쪽으로 두께 입히기(targets 선택). **shade** — auto(각도 기준 자동 스무스)/smooth/flat.
- **lightmap_uv** — 유니티 라이트맵용 UV2(이름 Lightmap) 겹침 없이 깔기, 이미 있으면 건너뜀, 기본 UV 불변.
- **rename** — 오브젝트·재질 한글→ASCII 일괄 개명(prefix 선택, 매핑 보고). 본 이름은 애니·웨이트 연결 탓에 불가침(정직 고지).
- **normals** — 세션61 노하우 도구화: 메시별 signed volume 음수=뒤집힘 진단→recalc(+필요시 전체 플립)→전/후 수치 보고.
- **align** — ground(바닥 스냅)/row(일렬)/grid(격자), 조립품은 자식까지 통으로 측정.
- **purge** — ⭐0-user 데이터는 저장 때 어차피 버려짐 → 실파일 찌꺼기의 정체는 **fake user만 잡고 있는 데이터**(임포트 딸려온 액션 등). 기본은 후보 보고만, include_fake=true일 때만 삭제.
- **pose_apply** — poses JSON(frame·bones{rot_deg·loc})→키프레임, 액션 개명, 없는 본 정직 보고. 설계=클로드/적용=루시 분업.
- **physics_bake** — rigid(낙하 시뮬→키프레임 굽기, 임시 바닥 자동)·cloth(천 드리워 모양 고정, target 또는 최상단 메시). ⭐**헤드리스 함정: bpy.ops.rigidbody.bake_to_keyframes는 내부 keyframe op가 poll 실패로 죽음(실측)** → 프레임 밟으며 평가 행렬 기록→시뮬 제거→데이터 API keyframe_insert로 직접 굽기.
- 실물 E2E 14/14(러너 레벨 수치 검증: 뒤집힌상자 vol -1→+1, 강체 3개 z 2.0~4.4→0.4 안착, 천 꼭대기 1.6→1.06 등) + 미리보기 눈검수 4장 + 도구 레벨 2종(확인 게이트·요약문·purge 정직 보고) + 작은 두뇌 키워드 게이트("법선"·"격자로 정렬") 통과.
- 파일: `blender_runner.py`, `blender3d.py`, `tools.py`, `knowledge/manual_루시_사용설명서.md`.

---

## 세션 62 3부 — 2026-07-17 밤: 유니티 C# 능력 강화 (도구 69 → 73종)

**요약:** "루시 C# 프로그래밍 능력 올려줘(유니티용)" — 핵심은 **편집-검증 루프를 분 단위→초 단위로**.

- **unity_cs_check** — dotnet가 유니티 DLL(Managed/UnityEngine·UnityEditor+모듈)+게임 어셈블리(Library/ScriptAssemblies)를 참조해 .cs 하나를 컴파일 검사. **유니티 부팅 없음·에디터 열려 있어도 됨·실측 1.5~4초**(unity_run 6~21초+잠금 대비). netstandard2.1+C#9 임시 csproj를 memory/unity_cscheck/에 캐시, CS0436(원본 vs 참조 어셈블리 중복 타입) 억제. ScriptAssemblies 없으면 정직 안내.
- **unity_cs_write / unity_cs_edit** — 프로젝트 Assets C# 전용 쓰기/부분수정(백업+확인, Assets 밖 탈출 차단) + **저장 직후 자동 cs_check 통합** → "쓰고→검사→고치고→재검사"가 몇 초 루프.
- **unity_context** — 클래스 맥락 팩: 정의 파일 개요+쓰는 곳들(파일당 대표 1줄)+네임스페이스·using 관례. 환각(없는 메서드 지어내기) 방지 층.
- **knowledge/unity_csharp_치트시트.md** — Unity 6 폐기 API(FindObjectOfType→FindFirstObjectByType)·프로젝트 관례(namespace DietCreature·신 Input System)·매 프레임 비용 규칙·CS 에러 해석·작업 순서. search_knowledge 조향에 "유니티 C# 짜기 전 치트시트" 추가.
- 조향 갱신: unity_new_script("내용은 cs_write/cs_edit로")·unity_run("빠른 확인은 cs_check 먼저, 이건 최종 검증").
- 실물 E2E: context 팩이 CreatureInteract 실코드 개요+사용처 2곳 출력, cs_write(고의 CS0103)→1.5초 검출→cs_edit 수리→1.6초 통과, 게임 클래스 참조(Assembly-CSharp)+Unity 6 API(FindFirstObjectByType) 컴파일 실증.
- 파일: `unity.py`, `tools.py`, `knowledge/unity_csharp_치트시트.md`, `knowledge/manual_루시_사용설명서.md`.

---

## 세션 62 2부 — 2026-07-17 저녁: 유니티 5종 (도구 64 → 69종)

**요약:** "유니티 5종 더" — 빈틈 기준 제안 5종 전부 승인. 감사→수리 짝 완성 + 3D 검문소 + 성능 관점 + 실행 수준 검증.

- **unity_tex_fix** — unity_audit·sprites가 잡은 큰 텍스처의 **수리 짝**: maxTextureSize 초과 .meta 일괄 하향(플랫폼 오버라이드 포함·원본 그림 불변·원본 픽셀이 작으면 건너뜀·자동 백업·확인 후·에디터 열림 거절).
- **unity_models** — 3D 모델 임포트 감사(unity_sprites의 3D판): Read/Write 켜짐·스케일≠1(블렌더 apply 누락 신호)·카메라/조명 임포트·Legacy 애니·.meta 누락·5MB↑. 블렌더 41동작에서 나오는 FBX의 받는 쪽 검문소.
- **unity_scene_lint** — 씬 성능 린트(모바일): 카메라·AudioListener 중복, 실시간 그림자, 조명·Canvas 과다, 무거운 씬. 씬 YAML 집계, 유니티 안 띄움.
- **unity_scene_smoke** — 빌드 씬을 배치모드로 **실제 로드**해 누락 스크립트·**끊긴 참조**(SerializedObject 스캔)·로드 예외를 잡음. 임시 에디터 스크립트(LucySmokeTemp.cs)를 심었다 지움. run_batch에 return_log 옵션 추가(마커 파싱용).
- **unity_code_lint** — C# 위생: Update류 본문만 중괄호 짝으로 오려내 매 프레임 비용 패턴(Find/GetComponent/Camera.main/Instantiate)을 그 안에서만 탐지 + 빈 Update·빈 catch·SendMessage·Debug.Log 집계.
- 실물 E2E: saladfarm 씬 린트가 Farm.unity 실시간 그림자 실검출, code_lint가 FarmTouch.cs Update 안 Camera.main·GetComponent 실검출, tex_fix 후보 6장(배경 941×1672) 실스캔(적용은 화질 트레이드오프라 사용자 판단 몫으로 보류), 가짜 프로젝트로 tex_fix 적용(meta 3곳 하향·작은 텍스처 보호·백업)·models 심은 문제 4건 전부 검출, **scene_smoke는 saladfarm 실물 배치모드로 7씬 전부 로드 통과**·임시 스크립트 청소 확인.
- **후속(같은 저녁, "캐시랑 그림자 문제 고쳐줘")**: Farm.unity Directional Light 그림자 OFF(yaml_edit로 m_Shadows 2→0, 2D 씬이라 시각 변화 없이 비용만 제거, 스모크 재검증 통과). FarmTouch.cs는 열어 보니 **이미 캐시된 코드**(Camera.main=null 복구 가드·GetComponent=클릭 프레임만) — 린트 오탐이라 코드는 안 건드리고, 대신 **code_lint에 오탐 억제 2종**(`== null` 복구 가드 줄·입력 게이트(wasPressedThisFrame 등) 뒤 코드는 제외) 추가 → saladfarm 재린트 깨끗 ✅.
- 파일: `unity.py`, `tools.py`, `knowledge/manual_루시_사용설명서.md`.

---

## 세션 62 — 2026-07-17 저녁: 블렌더 10종 (31 → 41동작, 세션61 승인 백로그)

**요약:** blender_3d에 anim_merge·bake·beauty_render + 수술 6종(repair·boolean·curve_path·sockets·uv_atlas·material_pbr), blender_batch에 contact_sheet. 도구 수는 그대로(64종) — 기존 두 도구의 동작 확장.

- **anim_merge** — 애니 FBX 여러 개(믹사모류)를 리깅 캐릭터 한 FBX의 클립들로 합본. 본 절반 미만 일치=정직 건너뜀, 재임포트로 클립 수·이름 실측 검증. ⭐블렌더 5.0 슬롯 액션에선 '모든 액션' 옵션 대신 **전 액션을 NLA 스트립으로 깔고 use_nla_strips=True**.
- **bake** — 하이폴리→로우폴리 normal/AO 베이킹(Cycles CPU). high/low 생략 시 폴리 수로 자동 판별, PNG는 ASCII·외부 파일 방식.
- **repair** — check의 수리 짝: 중복정점→퇴화면→구멍 메우기→뜬점→법선, 전/후 수치 비교. remesh=true면 복셀 리메시(최후수단).
- **uv_atlas** — 여러 오브젝트 재질을 아틀라스 한 장으로 베이크(재질 N종→1종). ⭐원본 UV=active_render(읽기)·AtlasUV=active(굽기)로 갈라야 색이 제대로 구워짐.
- **boolean / curve_path / sockets** — items 스펙 CSG(exact)·좌표 경로 pipe/ribbon/커브 배열·부착점 Empty(ASCII 강제, export 3종에 EMPTY 포함시킴).
- **beauty_render** — 기획서용: 3점 조명+섀도 캐처 바닥+투명 배경 PNG(Cycles).
- **material_pbr** — 텍스처 폴더의 albedo/normal/rough/metal/ao 자동 매칭→Principled 배선(Non-Color·노멀맵 노드·AO 곱·pack).
- **blender_batch contact_sheet** — 폴더의 3D 파일 전부 1컷씩 렌더→파일명 라벨 몽타주 한 장(비-blend는 새 shot 액션).
- ⚠️E2E가 잡은 함정 3건: ①PBR 매칭은 접미사(마지막 토큰) 우선 — `metal_albedo.png`의 metal 오탐 실측 ②**Cycles 베이크 후 같은 부팅에서 EEVEE 렌더=ACCESS_VIOLATION**(결정적) → uv_atlas 미리보기는 별도 부팅 ③헤드리스 FBX 라이트 크래시(세션59) 몽키패치를 safe_fbx_import로 상시화.
- 실물 E2E: 러너 10/10 + 도구 레벨 9종 + contact_sheet 3/3컷 + 눈검수(불리언 구멍·노멀맵 굴곡·아틀라스 색·투명 배경 알파 실측).
- 파일: `blender_runner.py`, `blender3d.py`, `tools.py`, `knowledge/manual_루시_사용설명서.md`.

---

## 세션 61 — 2026-07-17: 유니티 정찰 5종 + 관리 5종 (도구 54 → 64종)

### 1부 · 정찰 5종 (54 → 59)

**요약:** 유니티를 안 띄우고(전부 읽기전용·확인 없음·에디터 켜둔 채 됨) 프로젝트를 정찰하는 5종 추가.

- **unity_scene** — 씬·프리팹 YAML 파싱으로 게임오브젝트 계층+컴포넌트 트리(MonoBehaviour는 스크립트 클래스 이름으로, 패키지 캐시 guid까지 해석). path 없으면 씬·프리팹 목록.
- **unity_refs** — 에셋 guid 참조 추적(어디서 쓰나) + 미사용 에셋 후보 보고(Editor·Resources·Plugins 제외, Resources.Load는 못 잡음 경고 포함).
- **unity_settings** — 제품명·버전·번들ID·에디터 버전·빌드 씬 목록·define 심볼·패키지 요약.
- **unity_log** — Editor.log/Player.log의 **런타임 예외**를 종류별 횟수+스택으로 집계(unity_status=컴파일 에러만 보던 빈틈을 메움). player는 회사·제품명을 ProjectSettings에서 자동으로 읽어 LocalLow에서 찾음.
- **unity_outline** — C# 클래스·메서드 시그니처·직렬화/공개 필드 개요(unity_find=grep의 구조 버전). path 없으면 프로젝트 전체 파일별 요약.
- 작은 두뇌 도구 선별(_TOOL_GROUPS)의 유니티 게이트에 5종+키워드(미사용·예외·널레퍼·계층 등) 추가.
- 실물 E2E: saladfarm에서 7종 시나리오 전부 통과(씬 트리·프리팹 인스턴스 부모 연결·참조 5곳·미사용 44건·설정/빌드 씬 7개·가짜 로그 예외 집계 2종 3회).
- 파일: `unity.py`, `tools.py`, `knowledge/manual_루시_사용설명서.md`.

### 2부 · 관리 5종 (59 → 64) — 추천 5종 중 "폰 연동(adb) 빼고" 승인, 차점 스프라이트 감사로 대체

- **unity_snapshot** — 씬·프리팹·에셋·설정 스냅샷/목록/복원(`memory/unity_snapshots/`, 최근 15개). restore는 확인 후 + **복원 직전 상태 자동 백업**.
- **unity_diff** — 스냅샷(기본 최신) 또는 두 파일의 차이: 추가/삭제 게임오브젝트·바뀐 컴포넌트 값(fileID 기준 문서 비교).
- **unity_yaml_edit** — 씬·프리팹·에셋 직렬화 값 find→replace(자동 백업+확인). 에디터 열림 시 덮어쓰기 경고 내장.
- **unity_build_report** — 빌드 로그의 카테고리별 용량·큰 에셋 상위·직전 빌드 대비 증감(이력 10회). `run_batch`가 executeMethod 로그 사본을 `memory/unity_lastbuild_*.log`로 남기도록 수정.
- **unity_sprites** — 스프라이트 임포트 감사: 폴더 내 크기 이탈(통일 무리 ≥3장일 때만)·PPU 불일치·밉맵 켜짐·maxTextureSize 초과.
- ⚠️E2E에서 잡은 버그 2건: ①같은 초에 스냅샷 두 번 → 폴더명 충돌로 **복원 전 안전 백업이 원본 스냅샷을 덮침**(복원 후 내용 되읽기 검증으로만 발견 — 폴더명 `_2` 접미로 수리) ②스냅샷 id 접두 매칭이 `_2`·`_3`과 모호(정확 일치 우선으로 수리).
- 실물 성과: saladfarm 스프라이트 감사에서 swamp.png 941↔940 오프바이원, 아이콘 126↔128 혼재, PPU 64↔100 섞임 등 5건 실검출.
- 파일: `unity.py`, `tools.py`, 매뉴얼.

---

*세션 58~60은 당시 CHANGELOG를 빠뜨려 2026-07-17(세션 61)에 소급 기록. 상세는 메모리 `project_my_agent.md`의 해당 절.*

## 세션 60 — 2026-07-16~17

**루시(my-agent) 코드 변경 없음** — 알약게임 주사기(injector) FBX 납품 세션.
데칼 '통째 투명' 증상의 뿌리 2개 규명: ①블렌더 Alpha 소켓 연결=FBX 투명 재질(유니티 쪽 열쇠는 Extract Materials)
②데칼 면 노멀 뒤집힘(dot=-1 실측)→bmesh normal_flip 후 재수출·눈검수. 재납품 `Desktop\알약게임\injector_유니티용\`.

## 세션 59 — 2026-07-16

**요약: blender_3d 21 → 31동작 + 확장 4종** (도구 수 54 유지 — blender_3d 내부 action 확장).

- bone_template(humanoid/quadruped 표준 뼈대 배치, bind=웨이트까지).
- 수술 8종: mirror·array(linear/radial)·scatter·collider·materials·split·anim_edit·weight_transfer + text3d(한글 3D 간판)·compare. E2E 20/20.
- 확장 4종: **anim_preview**(애니→GIF, 없으면 턴테이블)·**lod**(`_LOD0/1/2` 한 FBX)·**build**(프리미티브 그레이박스)·**assemble**(여러 파일 씬 배치). E2E 9/9+눈검수.
- 파일: `blender3d.py`, `blender_runner.py`, `tools.py`, 매뉴얼.

## 세션 58 — 2026-07-16

**요약: 유니티 텍스처 유실 근절 + 신뢰성 2건** (도구 수 54 유지).

- 1부: blender_3d **unity_export**(텍스처명 ASCII화+pack, bake_decal 데칼 굽기).
- 2부: 슬래시 명령(/읽기 볼륨 등)을 persona에 안내 — 루시가 자기 기능을 "없다"고 하던 구멍 수리.
- 3부: **_ensure_ollama** — 로컬 두뇌 호출 직전 서버 확인·자동 기동(포트 폴링 15s). "모든 모델이 실패" 근절.
- 4부: ⚠️정정 — FBX 임베드는 URP에서 텍스처를 못 물림(실물 유니티 배치모드 검증) → **외부 파일 방식(FBX+PNG 동봉)**으로 전환.
- 파일: `blender3d.py`, `agent.py`(persona·_ensure_ollama), `tools.py`.

---

## 세션 57 — 2026-07-16

**요약: 도구 41 → 53종.** 자기 계측 · 코딩 작업실(파이썬·C#) · 유니티 C# 개발 도구 ·
블렌더/유니티 보완 · 이식성 향상. 새 모듈 3개(`coding.py`·`unity.py`·`portable.py`).

### 1부 · 자기 계측 (관측성)
- `status.record`가 성공 **횟수만** 세던 것을 확장. `memory/metrics.json` 신설(14일 보관).
- `status.record_call(label, 초, 순위)` — 응답시간 기록, 순위>0이면 **폴백** 집계.
- `status.record_fail(label, 종류)` — 실패를 종류별로(한도·길이초과·끊김 등).
- `agent.call_model`에 계측 훅. `/상태`에 **[계측]** 섹션(두뇌별 평균/최대 응답시간·폴백·실패내역).
- 파일: `status.py`, `agent.py`.

### 2부 · 코딩 작업실 (파이썬 + C#)
- 새 모듈 `coding.py` + 도구 6종: **code_write · code_read · code_edit · code_run · code_list · code_install**.
- `my-agent/workspace/`에 여러 파일짜리 프로그램을 짓고→돌려보고→에러 고쳐 재실행하며 완성.
- 격리: `workspace/` 밖(.. 탈출·절대경로) 차단. 파일 쓰기는 확인 없이, **실행만 프로젝트당 세션 1회 확인**.
- `code_run`은 `.py`=파이썬, `.cs`=dotnet(C#, csproj 자동 생성). 자식에 `PYTHONUTF8=1`.
- 파일: `coding.py`(신규), `tools.py`, `agent.py`.

### 3부 · 유니티 배치모드 연동
- 새 모듈 `unity.py` + 도구 **unity_run**: Unity를 창 없이(배치모드) 띄워 컴파일 점검·테스트(EditMode/PlayMode)·executeMethod 실행 후 로그 반환.
- `config.json`에 `unity` 블록(exe·projects 별칭). 파일: `unity.py`(신규), `tools.py`, `config.json`.

### 4부 · 유니티 C# 코딩 능력 강화
- **unity_find** — 프로젝트 Assets의 `.cs`를 실시간 검색(파일:줄), 기존 코드 파악.
- **unity_new_script** — 컴파일 되는 골격 생성(mono·scriptable·editor·test+asmdef·plain).
- **unity_run 컴파일 에러 정밀 파싱** — `파일(줄,칸) CS코드: 메시지`로 정리.
- EditMode 테스트 자동화(스캐폴드+어셈블리→실행). 파일: `unity.py`, `tools.py`, `agent.py`.

### 5부 · 블렌더 · 유니티 보완 4종
- **unity_status** — 에디터를 켠 채 `Editor.log`로 현재 컴파일 에러 확인(unity_run은 에디터 열려 있으면 실패).
- **blender_batch** — 폴더의 여러 3D 파일에 같은 작업(info·convert·prep_unity·수술) 일괄, 원본 불가침.
- **unity_build** — 배치모드 APK/AAB 빌드(kind=apk·aab·dev → `config unity.build_methods`).
- **blender prep_unity** — 유니티용 원클릭(apply+바닥원점+cleanup+FBX) + **자동 검증**(scale=1 확인). blender_3d의 새 action.
- 파일: `unity.py`, `blender_runner.py`, `blender3d.py`, `tools.py`, `config.json`.

### 6부 · 이식성 향상 (다른 PC로 옮겨도 되게)
- 새 모듈 `portable.py`: `expand()`(~·환경변수 + 다른 사용자 이름→현재 홈 자동 교정) ·
  `find_blender()` · `find_unity(version)`(Unity Hub·흔한 위치 자동 탐지, 프로젝트 버전 매칭) · `project_unity_version()`.
- 배선: 블렌더/유니티 실행파일 자동 탐지, `allowed_dirs`·유니티 프로젝트·문서검색 루트·백업·`.claude` 경로 전부 `expand` 경유.
- `config.json`은 변경 안 함 — 코드의 자동 교정이 옮겨진 PC에서만 맞춤(현재 PC 권한 불변).
- `setup_check.py`에 `check_dev_tools`(블렌더·유니티·dotnet·ffmpeg 탐지 보고) 추가.
- 파일: `portable.py`(신규), `blender3d.py`, `unity.py`, `tools.py`, `docsearch.py`, `knowledge.py`, `backup.py`, `setup_check.py`.

### 7부 · 블렌더·유니티 심화 — 감사/QA + 효율 (도구 53 → 54)
- **blender_3d `check`** — 유니티 반입 린트(스케일 미적용·UV없음·n-gon·비매니폴드·원점 어긋남·재질과다) **진단만**, 읽기전용.
- **unity_audit** — 유니티 안 띄우고 파일 스캔: **깨진 스크립트 참조** · **큰 텍스처**(빌드 용량) · `.meta` 누락.
- **blender_3d `chain`** — 여러 작업(apply·cleanup·decimate·uv·origin·join)을 **한 부팅**에 + export까지(콜드부팅 절약).
- **unity_run 잠금 프리플라이트** — `Temp/UnityLockfile` 감지 시 헛부팅 없이 즉시 안내.
- 파일: `blender_runner.py`, `unity.py`, `tools.py`, `agent.py`. 원칙: 눈 없는 루시=기계·숫자·반복 / 시각·조형=클로드.

### 검증
- 실물 E2E 통과: 파이썬·C# 편집-실행-디버그 루프, 유니티 배치모드(실 OnlyUpRat — 컴파일·CS 에러 파싱·EditMode 테스트),
  블렌더 일괄 info·prep_unity·check·chain(실 파일, 검증 후 정리), 이식성(실 설치 자동 탐지·사용자 경로 교정),
  unity_audit(실 프로젝트에서 큰 텍스처 실제 포착)·잠금 프리플라이트.
- 자가 점검(`doctor`/`/점검`) 9항목 전부 정상.

### 백업
- `config.json.bak_s57` · `.bak_s57b` · `.bak_s57c` (unity 블록·build_methods 추가 전 스냅샷).
