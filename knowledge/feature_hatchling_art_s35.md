---
name: feature-hatchling-art-s35
description: "세션35: 기본 단계(해칠링) 아트 22종 사용자 신규 이미지로 교체 + 수면 스프라이트 재생성. 걷기 프레임은 기본 단계에 원래 없음."
metadata: 
  node_type: memory
  type: project
  originSessionId: b7ab036b-e49e-4281-9b1d-4bd6439cda55
---

# 해칠링(기본 단계) 아트 22종 교체 (2026-07-09 세션35)

사용자가 `Desktop\해출링\pre_<이름>.png`(1024px, removebg RGBA)로 22종을 가져옴: bd1 bd3 bi2 bm2 bv1 bv2 bv3 frog pd3 premium2/4/5/10 pv1 pv3 rare2/5/6/10 rd1/2/3. 성장 21종 목록에 **bd1이 추가**된 구성.

- **변환**: 알파 이진화(≥128) → bbox 크롭 → **48그리드 BOX 다운스케일 + 내부 구멍 메움 + 32색 양자화** → ×2 NEAREST → 128 캔버스, **하단 여백 14로 통일**(기존 기본 아트는 14~24로 제각각이었음). 스크립트 `scratchpad/hatch/convert.py`.
- **방향 함정**: bv3·pd3·rare6이 좌향처럼 보여 반전했더니 오히려 뒤통수/반대로 어색 → **반전 없이 원본 그대로가 정답**(FLIP=set()). 기존 우향 통일 규칙을 기계적으로 적용하지 말고 반전 전/후 비교 이미지를 볼 것.
- **⚠️원본 폴더 `Desktop\해출링`은 세션 도중 사라짐**. 이후 재작업은 `Assets/Art/Creatures/<n>.png`(128px 도트)에서 직접 한다. 전 종이 완벽한 2×2 도트 블록이며 **x 위상만 종마다 0/1로 다르다**(y는 항상 0) → 도트 좌표계로 내릴 땐 위상 자동 검출 필수(`sleep_v3.detect_phase`).
- **수면 재작업(v6 = `scratchpad/hatch/sleep_v6.py`, 최종)**. 시행착오가 요점:
  - v1 동공만 지움 → 눈 둘레 **어두운 안티에일리어싱 링**이 남아 얼룩
  - v3 링 포함 + **세로 보간** → 이마 그늘이 눈 밑으로 끌려와 **다크서클**
  - v4 링 포함 + **가로 보간** → frog는 옆의 주황 배가 눈 위로 번짐
  - v5/v6 **경계 인페인팅**(경계에서 한 겹씩, 바깥 이웃색 평균) → 방향 편향 없음. 최종 규칙 4개:
    ① 눈 = 외곽선(투명에 닿는 어두운 픽셀 flood) 제외한 어두운 덩어리, **bbox 전체를 지움**(premium2는 하이라이트가 동공 스팬 밖으로 걸침)
    ② 헤일로는 **눈에서 8방향 3도트 이내**만(무제한이면 음영을 타고 **입·코까지 먹음** — rd3·rare2)
    ③ 헤일로 판정 = 얼굴보다 어둡고 **눈색↔얼굴색 선분에서 45 이내**(이 조건이 없으면 흰 얼굴 종의 **분홍 볼터치가 먹힘** — premium10·premium5)
    ④ 인페인팅 소스는 **얼굴색과 색거리 100 이내**인 이웃만(없으면 아무 이웃) — frog의 주황 배 유입 차단
  - 21종 자동 성공, **rare5(알 껍질 틈새 속 눈)만 검출 0이라 v1 결과 유지**. 백업 `ArtBackups/s35_sleep_v1`(v1), `s35_sleep_v3`(v3).
  - 검증: 44파일 128×128 · 하단 114 · **깨어있는 아트와 알파 실루엣 완전 일치**.
- (구) v1 방식: 어두운 클러스터 눈 검출(외곽선은 투명 이웃 접촉률 0.18 초과로 제외) → 행 단위 좌우 선형보간 채움 → 1px 포물선 ‿(눈색 75%+살색 25%). 자동 검출 실패 4종을 개별 처리:
  - `premium10`(그릇을 눈으로 오검출) → 클러스터 크기 상한(불투명 8%·폭 35%) 추가
  - `premium5`·`rd2`(파스텔 소용돌이 얼굴) → 세로 그라데이션 보간(위/아래 경계행 선형보간)으로 무늬 유지
  - `rare5`(알 껍질 틈새 속 눈) → 틈새 내부를 외곽 플러드필 도달 불가 영역으로 판정해 어둠색으로 메움
- **걷기 프레임 불필요**: `CreatureCatalog.GetWalkFrames`에서 stage0은 `CreaturesWalk/`를 보는데 그 폴더엔 원래 snow_spirit 2장뿐 → 기본 단계는 정지형이 정상. 코드 수정 없음.
- **백업**: `Documents\DietCreature\ArtBackups\base_s35_old\{base,sleep}` 각 22장.
- 검증: 44파일 128×128·.meta 유지·하단 정렬 114 일치. **플레이모드/실기기 육안 확인은 다음 APK**.
- 관련: [[feature-stage1-art-s33]], [[feedback-creature-art-pipeline]], [[feature-creature-sleep]]
