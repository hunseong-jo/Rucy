---
name: project-pill-base-model
description: 알약 협동 게임의 맵 중앙 기지(뚜껑 열린 알약 탐사선) 모델 현황 — 크기 확정과 작업 방침
metadata: 
  node_type: memory
  type: project
  originSessionId: 3e1a97a8-0367-4c4d-bad8-40e92063ea17
  modified: 2026-07-22T02:57:47.163Z
---

알약 협동 게임([[project-pill-coop-game]])의 **맵 중앙 기지** = 뚜껑이 열린 알약 탐사선. 플레이어들이 타고 위(胃) 안에 착륙했다는 설정. 조형은 **사용자 본인이 함**. 원본 `C:\Users\user\Desktop\알약게임\drug.blend`, 작업본 `drug_수정.blend`. 레퍼런스는 AI 생성 컨셉아트(원형 플랫폼 + 뒤에 세워진 뚜껑 + 툴랙 + 플레이어 4명).

**2026-07-19 최종 상태: 원본 형태 그대로 × 7.55배 스케일만 적용.**
- 걷는 바닥 지름 **14** (플레이어 7.8명 폭), 전체 23.3 × 15.1 × 29.6, 접지면 z=0, 1,828 tris.
- 형태·힌지·뚜껑 전부 무수정. 힌지 너클은 사용자가 만든 것이고 상태 좋음.
- 바닥 원판(`Circle`)이 그릇 테두리와 거의 같은 높이라 난간이 없지만, **사용자가 테이블(플랫폼)을 따로 만들 예정이라 무관**하다고 확인함.

**무기 테이블(2026-07-19, 클로드가 신규 제작)** — `Desktop\알약게임\무기테이블\`에 `weapon_table.blend` + `weapon_table_albedo.png` + `weapon_table.fbx`. 레퍼런스는 사용자가 준 `dasf.png`(로우폴리 나무 작업대). 크기 5.0 × 1.4 × 0.95(상판이 플레이어 허리), 108 tris, 상판 슬래브 + 에이프런 프레임 + 다리 4개 전부 각진 박스(레퍼런스가 그럼), 플랫 셰이딩.
- **팔레트: 전부 나무.** 상판 `A97B4F` / 옆면 띠 `8A6340` / 다리 `96693F` / 에이프런 `7E5B3A`(상판 그늘에 있으니 제일 어둡게). 다리 결은 기둥을 따라 세로, 에이프런은 보를 따라 가로.
  ⚠️처음엔 다리·에이프런을 **캡슐 뚜껑과 같은 청보라**로 했다가 사용자가 "청보라는 별로 안 어울린다, 차라리 다 나무가 낫다"고 해서 전부 나무로 교체(상판은 그대로 유지 요청). **청보라는 뚜껑에만 남기는 게 정답** — 기지 안에 파랑이 두 군데 있으면 산만해짐. 기지 팔레트=흰 외피(0.8,0.8,0.8)·연두 바닥(0.745,0.80,0.23)·청보라 뚜껑(0.227,0.171,0.8)이고, 나무의 온기가 이 셋과 대비되어 잘 붙음.
- **UV는 1024 아틀라스 4구역 수동 평면투영**(스마트 언랩 안 씀 — 텍스처를 PIL로 절차생성하니 구역을 직접 통제해야 예측 가능): A(0,0.5~1,1) 나무 상판·B(v 0.375~0.5) 나무 옆면 띠·C(u 0~0.5, v 0~0.375) 청보라 금속 다리·D(u 0.5~1) 에이프런. 면 법선의 우세축으로 투영축을 고르고 각 면 bbox를 구역에 맞춤, **구역 경계 3px 인셋**으로 필터링 번짐 방지.
- ⚠️**PIL은 블렌더 번들 파이썬에 없음** — 시스템 파이썬(PIL 12.2.0)으로 텍스처를 먼저 굽고 블렌더는 로드만 할 것.

🚨⭐**이 작업에서 배운 것 — 사용자 조형물은 고치지 말고 요청받은 것만 할 것.**
"부족한 게 뭐냐"는 진단 요청에 스케일 문제를 찾아낸 것까지는 맞았지만, 그 다음 몸통을 선반 프로파일 드럼으로 **재생성하고 뚜껑까지 반경·길이를 재조정한 것이 잘못**이었음. 사용자 반응은 "이상해, 그냥 내가 만든 알약 그대로 써주면 안돼?" / "힌지부분도 배치도 이상하고 엉망이야" — 전부 폐기하고 순수 스케일로 되돌림. **형태 변경은 승인을 받고, 기본값은 '건드리지 않기'.** 관련: [[feedback-verify-draft-before-building-on-it]]

⭐**유용했던 것: 새 모델 조형 전에 플레이어를 실측해 프록시로 올려볼 것.**
플레이어 실측 키 1.798(`납품완료\player_리깅\player_rigged_수정.blend`)인데 기지 바닥 지름이 1.854 — 바닥 전체가 플레이어 한 명 길이였음. 프록시 4명 + 툴랙을 올린 렌더 한 장이 즉시 드러냄. 스케일 진단은 이 방법이 가장 빠름.

⚠️**블렌더 스크립트 함정(실제로 밟음):**
- `bound_box`는 스크립트로 정점을 수정한 뒤 **갱신되지 않음** — 뚜껑이 bbox에서 통째로 빠져 보였음. 크기는 정점에서 직접 잴 것.
- bmesh `to_mesh()` + `free()` 뒤 BMFace 참조는 죽음(`ReferenceError`) → 면 인덱스는 생성 순서로 따로 저장.
- 돔/셸 형태에 **PCA로 축을 구하면 안 됨** — 주성분이 모호해 11.3°가 나옴(실제 58.9°). 오브젝트 원래 회전값에서 유도할 것.

**탐사선 디테일(2026-07-19, 클로드 추가) — `drug_디테일.blend`** (`drug_수정.blend`는 깨끗하게 보존). 전부 **별도 `Detail_*` 오브젝트로 추가**, 사용자 메시 4개(Sphere/Sphere.001/Sphere.002/Circle)는 tris 수로 무변경 검증(816/710/272/30). 신규 1,800 tris, 합계 3,628.
- `Detail_WindowFrame`(336) `Detail_LandingGear`(272) `Detail_Thrusters`(112) `Detail_Antenna`(72) `Detail_Lamps`(168) `Detail_HullBands`(768) `Detail_Ramp`(72). 재질 M_Detail_Metal/Trim/Glass/Lamp.
- **유리창은 불투명 스타일화로 전환**(연한 하늘색+약한 발광 0.35). 원래 alpha 0.05+transmission 1.0이라 안 보였고 **FBX가 투과를 못 실어감** → 데칼 때와 같은 처방(불투명하게 구우면 유니티 설정 자체가 불필요). 되돌리려면 Sphere.002 머티리얼만 교체.
- 램프 46°·수평 13.5·발끝 x=21.3, 림에서 바닥까진 **계단 3단**.
🚨⭐**최대 함정: 회전체의 축을 정점 centroid로 구하지 말 것.**
사용자가 "캡슐 중심이랑 추가 오브젝트 센터가 안 맞는다"고 지적 → 실측하니 몸통의 **진짜 축은 정확히 원점 (0,0)**(슬라이스 최소자승 원 피팅 잔차 0.0000)인데 내가 쓴 centroid는 **(−0.750, 0)** 이었음. 사발 메시의 힌지 쪽에 정점이 더 몰려서 무게중심이 끌려간 것. 붙인 디테일 전부가 0.75 밀려 있었음. **정답=z 슬라이스별 Kasa 원 피팅 후 중심 평균**, 검증=각 디테일 오브젝트의 xy centroid가 축에서 0.05 이내인지 출력(단, 방위각을 비대칭 배치하면 당연히 어긋나므로 균등 배치할 것 — 램프를 25/75/125/235/285/335로 뒀다가 1.55 어긋나 오경보). ⚠️참고: **사용자의 바닥 원판(Circle)은 축에서 0.228 어긋나 있음** — 그들 모델이라 그대로 둠.

⚠️**그 외 함정 4종:**
  1. **창도 뚜껑처럼 닫힌 셸**이라 경계 엣지 0개 → 테두리를 못 따라감. 해법=**축 방향 투영 후 2D 볼록껍질**로 실루엣 외곽선 추출(각도 비닝은 빈 구간이 안쪽 정점을 집어 r이 1.62까지 붕괴함 — 실측으로 발견).
  2. **튜브를 구간마다 따로 만들면 이음새에 틈**이 보임 → 링을 공유하는 연속 스윕(평행이송)으로 바꿀 것.
  3. **`max(반경) within ±0.6`은 곡면에서 반경을 과대평가** → 선체 띠가 몸통에서 떠 보임. 해법=0.30 단위 프로파일 빈 + 선형보간, 검증=띠 정점의 이격 실측(0.29 ≤ 튜브반경 0.24+여유).
  4. **짧은 거리에 반경차 큰 tapered_tube = 원반(바람개미)** — 안테나 접시가 그렇게 보임 → 작은 박스형 센서 헤드로 교체.
- 🔎남은 주관 이슈: 램프가 시각적으로 큰 편(기지 폭 17에 수평 13.5). 맵 기획 정해지면 재조정 대상.

**착륙/개방 애니메이션(2026-07-22, 클로드) — `Desktop\알약게임\drug_애니.blend` + `drug_애니.fbx` + `drug_애니_개발자안내.txt`.** 원본 `drug_디테일.blend`은 보존. 118프레임@30fps: ①닫힘+다리접힘(비행) → ②랜딩기어 4족 순차 전개(다리마다 4프레임 어긋내 오버랩) → ③뚜껑 열림(끝에 살짝 오버슈트). **뚜껑(Sphere) rot_y: 닫힘 0° ↔ 열림 -58.9°**(몸통 Sphere.001과 힌지 원점 (-7.553,0,12.839) 공유). **랜딩기어(단일 메시 Detail_LandingGear=4다리×(위·아래·발판)=12 loose part)를 4개 다리 오브젝트 Gear_PP/PN/NP/NN로 분리**, 각 다리 힙(반경~7.2·z5.8)에 피벗 Empty Piv_Gear_*를 두고 수평 접선축(=(-r.y,r.x,0)) 기준 quaternion으로 접음: **접힘 135°(SIGN+1, 다리가 hull 뒤로 완전히 숨음) ↔ 펴짐 0°(현재상태)**. **창틀 Detail_WindowFrame을 뚜껑(Sphere)에 부모로 묶음**(전엔 안 따라가 닫으면 허공에 뜸·유리 Sphere.002는 이미 자식). ⚠️**Blender5.0 애니 함정: action.fcurves 접근 금지(크래시)** → keyframe_insert만 쓰고 이징은 기본 베지어에 맡김. FBX=단색뿐이라 텍스처 경로 함정 무관, object_types={MESH,EMPTY}로 카메라·조명 제외, axis -Z/Y, bake_anim. **검증=빈 씬 재임포트→f1/f52/f118 렌더로 모션 살아있음 확인**. 유니티: Rig=Generic·Loop 끄기(개발자안내 참조).

**남은 것(미착수):** 유리창이 사실상 안 보임(alpha 0.05 + transmission 1.0, 게다가 **FBX는 투과/알파를 유니티로 못 실어감** — [[reference-fbx-inspect-s59]]). 진입 램프·해치, 탐사선 디테일(착륙 다리·스러스터·안테나). 폴리곤 여유 많음(1,828 → 히어로 구조물이면 8,000~15,000 가능).
