---
name: todo-lucy-feature-backlog
description: "루시 기능 백로그 — 맨 위 🔜'다음에 할 것'(세션64 정리: 관찰 뒤 조정 4·기존 후보 2·새 후보 4, 착수 전 사용자에게 물을 것). 세션63 승인 3건은 전부 완료. ⛔폰 원격·샐러드팜은 제안 금지"
metadata: 
  node_type: memory
  type: project
  originSessionId: 46d54f4f-1ff7-4127-9bb7-daf21213db1a
  modified: 2026-07-21T09:25:33.159Z
---

# 루시 기능 백로그 (2026-07-13 세션51, 사용자 "전부 추가할 기능으로 기억해줘")

## ✅ 세션65 (2026-07-19 새벽) 완료 — 아래 A2·A4가 여기서 해결됨
**눈 신뢰도 3순위 작업**(사용자 "순서대로 3개 진행"): ①🔧기계 판정 관문(vision.machine_verdict — 정확한 마젠타5%↑·단색·'어둡고 동시에 밋밋함'은 눈에게 안 묻고 확정. 블렌더엔 방어막이 아예 없었음. ⚠실측 오탐 1건 잡음: onlyuprat 배관 스테이지가 평균밝기 4.5라 걸렸으나 정상 렌더 → '어둡다'≠'검은 화면', std로 갈라야 함(진짜 실패 0.00 vs 정상 5.62), 실물 38장 오탐 0) ②시험지 수리 ③한도 재시도(쿨다운 뒤 재질문, `daily.eye_trust.max_wait_sec` 45분).
🚨**A2의 답: 눈이 아니라 시험지가 틀렸었다.** '바닥에 잠긴 상자'는 반투명 바닥이 **광택 바닥의 반사**로 읽혀(3D에서 반사는 정상) 세 눈이 독립적으로 '정상'이라 답한 것. 격자선을 상자 위로 지나가게 다시 그려도 소용없었음(Groq 2회 연속 '정상') → **문항 제거**. ⭐결론: 바닥 관통은 눈이 아니라 기계의 일(최저 z 측정). 새 시험지=5장(정상3: 바닥상자·두상자·**어둡지만 정상**/문제2: 공중부양·**일부만 분홍**).
🎉**A4의 답: Groq은 만점(믿음)이었다.** 한도로 못 재던 것뿐. 이제 자가검수 짝 = Groq(믿음)+nemotron(강등) 으로 설계대로 성립. nemotron 강등은 공정한 시험에서도 유지(놓침2·오탐1). Gemini는 이날 클로드가 진단하느라 일일 한도를 다 써서 0/5 '판정 보류' — **다음 정기 시험 때 재측정 필요**.
**두뇌 재배치+추가**: deep을 Gemini→**NVIDIA nemotron-3-super**(9.0초→1.0~2.7초). 근거=계측 4일치(한도 실패 20건 중 18건이 Gemini9+Groq9, 버벅임도 Gemini인데 호출 1위 49회 / NVIDIA 3종은 0건). **NVIDIA glm-5.2 추가(11단째)** — 후보 5종 벤치 1위(3/3·9.2초, deepseek-v4-pro 24.6초·qwen3.5-397b 162.9초·qwen3-next-80b 1/3·kimi-k2.6은 404). ⚠Cerebras는 context 8192라 deep 부적합. 🚨**bench.apply의 deep 자동선정 폐지(사용자 결정 "수동 고정")** — 옛 규칙이 `max(secs)`=가장 느린 모델에 deep을 줬는데 '느림'은 숙고가 아니라 인프라 지연이었고, 벤치 3과제는 애초에 어려운 질문 능력을 안 잼.
**문서 능력 3종**(사용자 "파워포인트 한글 엑셀 능력 올리자" → "세 개 다 순서대로"): **한글 .hwpx 쓰기 신설**(읽기만 됐었음. 구조는 한글 COM으로 실제 파일 뽑아 학습. ⚠본문이 글자·문단모양을 번호로만 가리켜 header.xml과 어긋나면 파일 거부 · mimetype은 zip 첫 항목+무압축) · **PPT 그림+진짜 표**(그림: 경로, 글 왼쪽·그림 오른쪽) · **엑셀 서식·수식·다중시트**(`# 시트이름`으로 분할, =SUM 실제 계산). 전부 라이브러리 없이 zip+XML. 검증은 한글·PowerPoint·Excel COM으로 직접 열어서 + 슬라이드 PNG 눈검수.
⭐**이 PC는 한글(Hnc Office 2018)·MS Office 둘 다 설치돼 있고 COM 자동화가 된다** — 문서 작업 검증에 계속 쓸 것.
🔜 남은 것: 한글 표(hp:tbl)는 아직 글줄로 펴짐 · Gemini 눈 재측정.

## 🔜 다음에 할 것 — 2026-07-18 세션64 말미 정리 (사용자 "나중에 해야하거나 하면 좋을거 정리해서 기억해줘")
**"루시 작업하자"가 나오면 이 절부터.** 세션63 승인 3건은 세션64에 **전부 완료**됐고, 아래가 지금 남은 전부다. 사용자가 아직 고르지 않았으므로 **착수 전에 무엇부터 할지 물을 것**(승인된 대기 목록이 아님).

### A. 관찰 뒤 조정 — ✅ 4종 전부 완료 (2026-07-21 세션68, 사용자 "A")
1. **🙋 사람 호출 빈도 보기** — ✅ **관찰 인프라 신설**(여태 발동을 아무도 안 세서 관찰 자체가 불가였음). `status.py`에 `record_consensus`/`consensus_lines` + `memory/eye_consensus.json` — 눈 짝별 합의/엇갈림을 세고 `/상태`에 최근 7일 표시, **표본 8건↑ & 엇갈림 40%↑일 때만** "짝 조절" 힌트. `tools._eye_look`·`_eye_look_many` 두 경로에 기록 심음(순서 무관 합산·비율 계산 검증). 이제 며칠 굴러야 실제 빈도가 쌓임 → 그때 조절안(믿는눈2개/2:1다수결/근거요구) 판단.
2. **눈 시험 '바닥에 잠긴 상자'** — ✅ 이미 세션65에 `sunken_box`를 시험에서 제거함(모든 눈이 틀리는=벌점만 무는 문항, `eyecheck.py`). 조정 불필요 재확인.
3. **자동 후순위 실발동** — ✅ 실행해보니 **0건**(최근 3일 실사용 두뇌 실패율 최고 Gemini 33% < 문턱 50%, 느림도 미달, silent_faults 0). 안전망이 설계대로 잠잠 = 정상. 단 `nemotron-3-super` avg 17.3s는 경계선(지켜볼 값).
4. **Groq 눈 재측정** — ✅ 재측정: **Groq은 45분 기다려도 3문항만 채점(판정 보류) — 무료 한도가 발목, 눈으로 신뢰 어려움**. 대신 **Gemini 3 Flash가 믿음 5/5로 승격**(이전엔 늘 한도로 측정불가), nemotron은 주의(분홍 놓침 여전). `eye_trust.json` 갱신, `vision.capable`이 Gemini를 앞에. → #1 짝 카운터가 앞으로 Groq 빈답/한도를 관찰하게 됨.

### B. 기존 후보(세션62부터 계속 남아 있던 것)
5. **웨이크워드 "루시야"** — 선행 조건(연속 음성 실사용)은 세션56에 풀렸다. openWakeWord(한국어 커스텀) vs Porcupine(무료 키 1개). 상시 마이크=프라이버시·CPU 부담을 먼저 고지하고 시작할 것.
6. **지하철 정식 키**(선택) — 지금은 sample 키로도 돌아감. `keys/seoul_subway.txt`.

### C. 있으면 좋을 것 — 세션64에 드러난 구멍에서 나온 새 후보
7. **실전 한 바퀴(계속 미뤄지는 것)** — 세션63~64 산출물 상당수가 아직 **픽스처 검증만** 됐다. 특히 🎓작품 지적 학습(판단력 3층)은 **실발동 0회**. 다만 ⛔샐러드팜은 사용자가 종료했으므로 무대는 **onlyuprat 또는 알약 협동 게임(블렌더 납품물)** 쪽이어야 한다.
8. **👉다음 순번(2026-07-18 사용자가 고름 — "그럼 나중에 하자"로 착수는 미룸)** **눈검수 결과를 교훈으로** — 🙋 사람 호출로 올라간 판정을 사람이 최종 판단하면, 그 결과를 lessons에 남겨 다음 판정 기준선으로 쓰는 것(판단력 3층과 자연스럽게 이어짐).
9. **doctor에 '작업 프로젝트' 항목 통합** — 지금 unity_health는 새벽 일과·브리핑 경로만 탄다. `/점검`에서도 한 줄 보이면 사람이 물었을 때 즉답이 된다(작음).
10. **문서·기획서 쪽은 현재 대기 없음** — 알약 협동 게임 기획서 잔여(게임명·탈출 구간·네트워킹·C-14 수치표)는 [[project-pill-coop-game]]에 있고, 그건 루시 기능이 아니라 **사용자+클로드 작업**이다.

### ⛔ 다시 제안하지 말 것
폰 원격 계열 일체(ntfy 푸시·LTE 검증·Tailscale HTTPS·WoL·웹 자동시작) · 손(computer.py) 강화 · adb/폰 개발 · IoT(기기 없음) · 일일 퀘스트류 콘텐츠 · **샐러드팜 개발 재개 제안**(2026-07-18 종료, 감시도 제외).

## ✅👁 자가검수 = 눈 2개 합의제 (세션64 말미, 사용자 지시 "눈 두개한테 물어보고 엇갈리면 사람에게 물어보게 해줘")
- `tools._eye_look`(한 장)·`_eye_look_many`(묶음) 둘 다 **서로 다른 눈 2개**(vision.capable 신뢰도 순)에게 묻는다. 둘 다 정상=합의 정상 / 둘 다 문제=합의 문제 / **엇갈리면 판정하지 않고 🙋 사람에게 넘김**(두 눈의 말+그림 경로 같이 출력). 눈 하나뿐이거나 둘째가 한도면 단독 판정 + **"⚠혼자 본 판정" 명시**. 끄기 `vision.two_eyes=false`.
- ⭐**왜 한쪽을 고르지 않나**: 기계가 어림짐작으로 고르면 눈 하나 쓰던 때의 위험(검은 화면을 '정상'으로 통과)으로 그대로 되돌아감.
- ✅**실물 E2E에서 엇갈림이 자연 발동**: 마젠타 화면에 Gemini="문제" / nemotron-nano-VL="정상" → 🙋 사람 확인으로 올라감(정상샷·검은화면은 2개 일치). 즉 **강등된 눈을 명단에 남겨둔 게 오히려 안전망으로 작동**(혼자 뒀으면 통과됐을 것).
- 🐞**같이 잡은 묵은 버그**: `_eye_look_many`(유니티 여러 씬 묶음 판정) 파서가 **이름으로만** 판정을 찾았는데, 실제로는 두뇌들이 `1번:`·`2.`처럼 **번호로 답한다**(Groq·nemotron 둘 다 실측 — Gemini만 이름을 씀). 즉 세션63부터 **묶음 눈검수 판정이 조용히 통째로 버려지고 있었음**(빈손이면 그냥 검수 줄이 안 붙어 아무도 몰랐다). 번호 형식(`^\d+\s*(번|.|:|\))`)도 받게 수리.
- ⚠️비용: 눈 호출이 2배. Groq 눈은 무료 한도가 빡빡해 자주 30분 쿨다운에 빠짐 → 그때는 Gemini+nemotron 조합으로 굴러감.

## 🔨 착수 대기 3건 (2026-07-18 세션63 말미 승인 — "1,2,3 위 모두 작업할 목록에 넣어줘")
클로드가 "다음에 뭐가 필요한가" 질문에 제안한 3건을 사용자가 **전부 승인**. "루시 작업하자"가 나오면 이 목록부터, 1→2→3 순.

**⚠️먼저 권한 것(사용자가 명시 승인하진 않음, 정황상 병행 가능)**: **실전 한 바퀴** — 세션63에 들어간 것 대부분이 클로드가 만든 픽스처로만 검증됐음. 특히 ①작품 지적 학습(3층)은 **진짜 사용자 지적으로 발동한 적 0회**(감지기 낱말이 실제 말투와 어긋날 수 있음) ②unity_shot play=true는 saladfarm에서 미검증(MCP 깨짐 탓, onlyuprat으로 대체) ③전/후 비교도 픽스처만. 실제 작업(샐러드팜·알약게임)을 한 바퀴 돌리면 어긋나는 지점이 나올 것 — [[feedback-verify-by-actually-running]] 그대로.

### ✅1️⃣ 작업 대상 프로젝트 건강 감시 — **완료(세션64, 2026-07-18 저녁)**
- **한 것**: `unity.health()`(도구 76종 `unity_health`) + 새벽 06:00 일과(`daily.unity_health`) + 아침 브리핑 한 줄(문제 있을 때만). 결과는 `memory/unity_health.json`(36시간 넘으면 브리핑이 안 씀 — 브리핑은 유니티를 절대 안 켬).
- ⛔**감시 제외(2026-07-18 사용자 결정)**: "샐러드팜은 더이상 안 해도 괜찮아" → config `unity.health_skip=["saladfarm","dietcreature"]`. 안 고칠 CS0115를 매일 아침 브리핑에서 읊으면 경보만 무뎌지기 때문. **별칭은 살려뒀으니 이름을 대고 시키면 여전히 점검됨**. 프로젝트 파일·기존 메모리는 그대로 둠(사용자가 '감시만 끄기' 선택 — 메모리에 '종료' 표시까지는 안 함).
- ⭐**설계 반전**: 원래 계획한 `unity_status`(Editor.log)는 **못 씀** — Editor.log는 PC에 하나뿐인 전역 로그라 어느 프로젝트 것인지 알 수 없음(실측: 로그가 엉뚱한 `D:\test\My project` 것이었음). 그래서 **프로젝트마다 배치모드로 열고 닫아** 진짜 컴파일 결과를 봄. 에디터가 열린 프로젝트는 **정적 점검**(최신 .cs mtime vs `Library/ScriptAssemblies` 최신 .dll mtime → 코드가 더 새것이면 ⚠stale)으로 자동 대체 → 잠금 사고 없음.
- ✅**실물 E2E가 진짜 구멍을 잡음**: saladfarm `error 1개` = `Library/PackageCache/com.ivanmurzak.unity.mcp@cd1ce29f6eab/Runtime/UnityMcpPlugin.Config.cs(148,50) CS0115` — **세션63의 그 깨짐이 아직 안 고쳐진 상태**(사용자가 MCP 패키지 업데이트/롤백해야 함). onlyuprat=ok. 픽스처가 아닌 실전 발견.
- ⚠️함정: 별칭 dedupe 필요(saladfarm·dietcreature가 같은 폴더) · 요약문에 ✗⚠✅가 들어가므로 **cp949 콘솔에 print하면 UnicodeEncodeError**(테스트 때 실측 — 루시 본체는 UTF-8이라 무관) · Bee의 `.dag.outputdata`엔 컴파일 메시지가 없음(빌드 메타뿐, 파싱 시도했다 버림).

### (원래 계획 원문) 1️⃣ 작업 대상 프로젝트 건강 감시
- **왜**: doctor 9종은 전부 **루시 자기 인프라**(멜로·ollama·스케줄러·웹·디스크·구글·ffmpeg·받아쓰기키·Tailscale)만 봄. 정작 **작업 대상 유니티 프로젝트가 컴파일도 안 되는 상태**는 아무도 안 봄 — 세션63에 saladfarm의 MCP 패키지가 CS0115로 깨져 배치 작업 전멸했는데 **루시는 몰랐고 클로드가 배치 로그를 읽어 발견**했음.
- **구현**: 새벽 일과(daily)에 `unity_status`(에디터 안 띄우고 Editor.log로 컴파일 에러 확인 — 이미 있는 도구) 한 번 + 결과를 **아침 브리핑에 한 줄**("샐러드팜 컴파일 에러 1건" 식). config unity.projects의 프로젝트들 순회.
- **⚠️함정**: **daily.py/config 저장하면 1분 내 실물 스케줄러가 물어감** — 새 일과는 daily.json 선표시 후 저장(기존 규칙). 에디터가 열려 있어도 unity_status는 되므로 잠금 걱정 없음.

### ✅2️⃣ 계측 자동 후순위 + 조용한 고장 경보 — **완료(세션64, 2026-07-18 저녁)**
- **한 것**: `status.rank()`가 `agent.call_model`의 기본 순서에 끼어들어 만성 고장·만성 지연 두뇌를 **이번 실행 동안만** 뒤로 미룸(60초 메모 캐시). `status.silent_faults()` → `/상태` [조용한 고장 경보] + **doctor 10종째**(새벽 자가점검이라 안 물어봐도 걸림). `/상태`에 [자동 후순위] 절·새벽 일과 줄 추가. config `metrics`(auto_demote·문턱들).
- ⭐**지켜야 할 원칙 3가지(코드 주석에도 박아둠)**: ①**config.json은 절대 안 고침** — 사람이 정한 순위를 조용히 덮어쓰면 안 됨, 런타임 재정렬만 ②**최후의 보루(로컬 Ollama)는 붙박이** — 인터넷 끊겼을 때의 마지막 답이라 앞뒤로 못 옮김 ③부른 쪽이 `order`를 준 경우(눈 전용 순서 등)는 개입 안 함.
- ⭐**문턱은 보수적**: 최근 3일·6번 이상 불린 두뇌만·실패율 50%↑, 또는 (표본 5↑ & 다른 두뇌 **중앙값의 3배** & 15초↑). 절대 초가 아니라 상대 비교인 이유=그날 인터넷이 느릴 수 있음. 경보는 더 엄격(4번↑ 중 60%↑). `시험 …` 임시 라벨(벤치 프로브)은 실제 두뇌가 아니라 제외.
- ⚠️**정직한 결과: 실측 데이터로는 아무도 문턱을 안 넘음**(최고가 Groq qwen3.6-27b 실패율 0.44/9회). 즉 **지금 당장 체감 변화는 없고**, 두뇌 하나가 진짜 맛이 갔을 때만 발동하는 안전망임 — 세션63의 예상("체감 개선 제한적") 그대로. 그래서 **가짜 계측 픽스처로 발동을 강제해 검증**함(후순위 2건·경보 1건·doctor False·순서 6→8번으로 밀림·로컬 맨 뒤 유지·config 무변). 실대화 1회 회귀 없음.

### (원래 계획 원문) 2️⃣ 계측 기반 두뇌 자동 후순위 + 조용한 고장 경보 (두뇌)
- **왜**: 세션57에 만든 `memory/metrics.json`이 **쌓기만 하고 안 쓰임** — 두뇌 순위는 여전히 수동 config. 세션63엔 눈 두뇌 429가 반복됐고 전부 계측에 남아 있음.
- **구현**: call_model이 metrics를 읽어 만성적으로 느리거나 실패하는 두뇌를 자동 후순위로 + `/상태`에 **조용한 고장 경보**(특정 두뇌만 계속 죽는데 폴백이 가려주는 상태).
- **⚠️정직 메모**: **체감 개선은 제한적일 수 있음** — 세션63 실측상 429가 반복돼도 폴백 사슬이 매번 잘 받아냈음. 급한 불 아님(그래서 2순위).

### ✅3️⃣ 눈(비전) 신뢰도 관리 — **완료(세션64, 2026-07-18 저녁)**
- **한 것**: `eyecheck.py` 신설 + 도구 `eye_trust`(77종) + 월1회 새벽 05:55 일과 + `/상태` [눈 신뢰도] 줄. 정답 아는 그림 6장(정상 2: 바닥에 놓인 상자·나란한 두 상자 / 문제 4: 공중부양·바닥에 잠김·검은 화면·단색 마젠타)을 **실제 👁 자가검수와 같은 어법**으로 물어 채점.
- ⭐**채점은 비대칭**: 놓침(문제인데 '정상')=치명 2배 벌점(불량이 조용히 통과), 오탐=가벼움(사람이 미리보기 보면 앎). 결과대로 `vision.capable()`이 **믿는 눈부터** 정렬하되 **강등해도 명단에서 빼지 않음**(눈 하나뿐일 수 있고 자가검수는 보조 신호).
- 🚨**실측 결과(2회 돌림)**: **NVIDIA nemotron-nano-VL**(세션63에 '눈 3단'으로 합격 등록한 그 두뇌)이 **강등** — 1회차 놓침 3(**완전한 검은 화면까지 '정상'**), 2회차 놓침 2(**단색 마젠타를 '정상'**). ⭐**회차마다 틀리는 문항이 바뀜 = 특정 약점이 아니라 그냥 일관성이 없음**. Gemini 3 Flash=놓침 1(바닥잠김)로 '주의'(2회 동일). Groq qwen3.6-27b=한도로 **판정 보류**(단발 호출은 잘 답함 — 무료 눈 한도가 빡빡, 이 두뇌는 다음 시험에서 재측정 필요). → **👁 자가검수의 "정상"은 그대로 믿으면 안 된다**가 실측으로 확인됨(반대로 "문제"라고 하면 실제로 문제일 가능성이 높음 — 오탐은 0건이었음).
- ⚠️**남은 의문(정직)**: '바닥에 잠긴 상자'를 **두 눈 다** 놓쳤는데, 이게 눈이 나쁜 건지 **반투명 바닥이 '반사'로 읽히는 애매한 문항**인지는 아직 못 가림. 다음에 이 문항을 손보거나 뜻이 더 분명한 대체 문항을 찾을 것.
- ⚠️**만들면서 잡은 함정 3개(전부 실측)**: ①시험지 '겹친 두 상자'는 **3D에서 앞이 뒤를 가리는 게 정상**이라 불공정 문항 → 뺌(클로드가 그림을 눈으로 검수해 발견) ②'바닥에 잠김'을 불투명 바닥으로 그리면 그냥 낮은 상자로 보임 → **반투명 합성**이라야 뜻이 하나 ③한 두뇌에 6장 연타=무료 분당 한도 429→**30분 쿨다운**에 남은 문항 전멸(Groq가 쉬운 2문제만 채점되고 '믿음' 받을 뻔) → **문항 바깥·두뇌 안쪽 순회** + `MIN_GRADED=4` 미만이면 '믿음' 금지(판정 보류).

### (원래 계획 원문) 3️⃣ 눈(비전) 신뢰도 관리
- **왜**: 세션63 눈시험에서 llama-3.2-11b-vision이 배경색·배치를 **틀리게** 답하는 걸 실측(그래서 등록 금지). 이미 등록된 눈들도 언제든 틀릴 수 있음. **위험한 방향은 "문제인데 정상이라고 하는 것"** — 반대(정상을 문제라 함)는 사람이 알아채지만 이건 조용히 넘어감.
- **구현**: 정답을 아는 그림 몇 장(마젠타 사각형·검은 화면·정상 씬 등)으로 눈들을 주기적으로 시험 → 오답 두뇌는 눈 명단에서 강등. 기존 `/벤치` 눈시험(세션46, vision 플래그 실측 자동갱신)의 확장으로 붙이면 됨. 월1회 자동벤치(05:50)에 얹는 것도 방법.
- **우선순위 낮은 이유**: 지금 눈검수는 **보조 신호**라 틀려도 치명적이지 않음(사람이 미리보기를 직접 봄).

**❌이번에 제외한 것**: **손(computer.py) 강화** — 유니티 GUI 조작은 클로드 MCP가 더 빠르고 안전, 손은 느리고 실패 시 엉뚱한 창 입력 위험. **adb·폰 계열** — 과거 기각 + 아래 ⛔ 중단 결정.

## ⛔ 폰(원격) 방향 개발 중단 (2026-07-18, 사용자 결정)
"컴퓨터를 켜지 않은 상태에선 못쓰는 게 너무 디메리트가 커서, 루시 폰은 그만 개발하자" — **PC가 꺼지면 무용하다는 근본 한계** 때문에 폰 원격 접속 계열은 추가 개발하지 않음.
- **중단(제안 금지)**: ntfy 폰 푸시(기존 1순위였음) · LTE 접속 검증 · Tailscale HTTPS(폰 마이크 🎤용) · WoL · 웹 자동 시작 등 폰 원격 계열 일체.
- **기존 것은 유지만**: web.py·PIN·Tailscale·/talk·업로드 등 이미 만든 건 지우지 않고 그대로 두되(PC 켜져 있을 때 집 안에서는 쓸 수 있음), 개선·확장 제안은 안 함.
- 남는 다음 후보: **웨이크워드 "루시야"**(PC 앞 사용이라 무관) · 지하철 정식 키(선택).

세션51에 코드 대조로 찾은 구멍·확장을 제안했고 사용자가 **7건 전부 승인** → **세션52(2026-07-13)에 7건 전부 완료**. 구현 상세·함정은 [[project-my-agent]]의 세션52 절(전반=①②③, 후반=④⑤, 3부=⑥⑦).

## ✅승인 10건 전부 완료 (세션54, 2026-07-14 — 구현 상세·함정은 [[project-my-agent]] 세션54 절)
신규 5: 지하철(get_subway, sample 키로도 동작·정식 키는 keys/seoul_subway.txt에 선택) · 폰 파일 내려받기 · 디스크 감시 · 웹 지난 대화(📜) · 월 1회 벤치(보고만, 05:50).
개선 5: doctor 웹 서버 점검+재기동(+_launch 잡탈출) · 멜로 예열(18.9→2.9초) · 브리핑 켜짐(08:00)+집앞버스 · /상태 백업·벤치 줄 · OpenRouter 건은 월벤치로 해소.

## ✅루시 자기 제안서 3건 완료 (세션54 후반, 도구 38종 — 상세는 [[project-my-agent]] 세션54 후반 절)
루시가 스스로 쓴 `Desktop\루시_추가_기능_제안서.docx`(13건)에서 승인 3건: **녹음 받아쓰기(transcribe_audio)** · **메일 초안(draft_mail — 초안함까지만, 발송은 사람)** · **캘린더 초대(add_event attendees)**. 나머지 기각/보류: GitHub·메신저·데이터 파이프라인(전제 없음), 이미지 리뷰·Unity 감시·일기·오프라인 동기화(기존 기능으로 이미 됨), 영상 요약(한도·GPU). 제안서의 '명상·스트레칭'·'간편 일기' 행은 본문 설명 없는 지어낸 행이었음.

## 사용자 확인 대기
- ✅**연속 음성 실사용(⑦) 완료(세션56, 2026-07-15)** — 사용자 육성 E2E 통과. 원인 2겹이었음: 기본 장치≠실제 마이크(목소리는 Galaxy Buds3) + 버즈 문턱 150 과대 → config voice.device="Buds"·talk.min_threshold=60. 상세·함정은 [[project-my-agent]] 세션56 후반. **웨이크워드 "루시야"의 선행 조건이 풀렸음** — 다음 제안 때 후보로 살릴 것.
- ✅**웹 PIN + Tailscale 폰 접속 완료(세션53, 2026-07-14)** — Tailscale 설치(winget)·로그인, **PC=100.74.78.102, 폰=z-flip4(100.102.162.125)**, 폰 PIN 로그인까지 확인(web_tokens 생성·트래픽 왕복 실측). 방화벽 OK(Tailscale 인터페이스=Private), web.py 0.0.0.0이라 코드 수정 0. 잔여: **와이파이 끈 LTE 상태 접속은 미검증**(원리상 동일, 사용자가 밖에서 한 번 확인하면 종결). ⚠️PIN을 클로드 화면에 출력하는 건 차단됨(자격증명) — 사용자가 keys/web_pin.txt 직접. ⚠️웹 서버는 재부팅하면 꺼짐(start_web.bat 수동) — 자동 시작은 추후 후보. ⚠️PC 꺼져 있으면 접속 불가('켜두기'로 합의, WoL은 나중에).

## 세션55 제안 5건 결과 (2026-07-15): ②③④ **완료** + 문서수정 강화(사용자 추가 지시) — 상세·함정은 [[project-my-agent]] 세션55 2부
- ✅**② 웹 음성 대화** — /talk+🎤 완료, 멜로 폐루프 E2E 통과. ⚠️**폰 마이크는 https 필요**: `tailscale serve --bg 8765`를 쓰려면 테일넷 HTTPS를 관리자가 한 번 켜야 함(사용자 몫 — serve 실행하면 활성화 링크 뜸: login.tailscale.com/f/serve). 켜기 전까지 🎤는 http에서 안내문만.
- ✅**③ 메일 챙길 것 추출** — 브리핑 [챙길 것 후보] 절, 실지메일 E2E. **내일 아침 브리핑이 실전 첫 무대**(확인해 볼 것).
- ✅**④ 문서 업로드** — accept 확장+경로 자동 입력, E2E 통과.
- ✅**(추가) 문서 수정 강화** — 워드 표 + **edit_document 도구(39종)**: 기존 docx/pptx 치환·docx 덧붙이기, 서식 보존, 자동 백업. E2E 18/18+COM 개봉+실전 조향 통과.
- **① 폰 푸시 알림(ntfy.sh)** — 미착수(사용자가 이번에 안 골랐음). 다음 후보 1순위로 유지: announce()에 HTTP POST 채널 하나, 폰엔 ntfy 앱.
- **⑤ 웨이크워드 "루시야"** — 보류 유지(⑦ /음성모드 실사용 검증 선행).

## ✅세션55 말미 '강화' 승인 4건 → 세션56(2026-07-15)에 전부 완료
"루시 작업하자" 발동 문구로 재질문 없이 착수(설계대로 작동). 구현 상세·함정은 [[project-my-agent]] 세션56 절.
2. ✅**웹 긴 작업 블록 해소** — 턴을 배경 스레드로, 20초 넘으면 pending id+폰 JS가 /poll 폴링. 긴 일 중에도 다음 질문 즉시 응답. E2E 10/10+실서버 적용.
3. ✅**PPT 수정 완성** — edit_document append가 PPT에 새 슬라이드를 닮(남의 덱 존중: 번호 최댓값+1·레이아웃 물려받기·sldSz 좌표). 실제 기획서 사본 24→25장+COM 개봉 검수. ⭐COMPACT 요약에서 append 조향이 잘려 있던 것도 발견·수정.
4. ✅**doctor 점검 6→9종** — 받아쓰기 키·ffmpeg·Tailscale(수리=up)·구글 토큰 권한(파일 scopes 직접 검사).
5. ✅**문서 색인 신선도** — 새벽 05:05 증분 색인+검색 빈손 시 즉석 증분(0.13초 실측).
(①알림 폰 푸시 ntfy는 이번에도 미선택 — 다음 제안 때 1순위 유지)

## ✅세션56 6~9부 완료 (2026-07-16) — 상세는 [[project-my-agent]] 세션56 6·7·8·9부
- 블렌더×루시 blender_3d(info·render·export) + **수술 8종**(apply·origin·scale_to·cleanup·uv·decimate·join·**auto_weight**=리깅 기계적 절반) + **convert**. 전부 사본에만·원본 불가침.
- ⚡**응답 속도 근본 수리 = tool subsetting**: 작은 두뇌엔 핵심17+질문 관련 도구만(고정 오버헤드 58%→24~30%, 평균 2543토큰/턴 절감). 실물 E2E 통과.

## ✅코딩 작업실 완료 (세션57, 2026-07-16) — 사용자 신규 요청 "프로그래밍 기능 강화"
- 방향 4개 제시(코딩 작업실·내 코드 리뷰·venv 실행환경·먼저 설명) → 사용자 **"코딩 작업실(짓기·고치기)"** 선택.
- coding.py + 도구 7종(code_write·read·edit·list·run·install + unity_run). my-agent/workspace/에 다중파일 프로그램 짓고→code_run→에러면 code_edit→재실행 루프로 완성. 격리(ws_path가 workspace 밖 차단), 파일쓰기 무확인·실행만 프로젝트당 세션1회 확인, PYTHONUTF8. **code_run .py=파이썬·.cs=dotnet(C#) 자동분기**. 상세는 [[project-my-agent]] 세션57.
- ✅**유니티 개발 확장 1차**(같은 세션, "지금 둘 다 해줘"): ①C# 콘솔 지원(code_run .cs→dotnet run, csproj 자동, dotnet 9.0.305) ②unity_run=Unity 배치모드 컴파일/테스트/executeMethod. Unity 6 D:\6000.4.5f1, saladfarm/onlyuprat. 실물 E2E 통과.
- ✅**유니티 C# 코딩 능력 강화 2차**(같은 세션, "유니티 c# 코딩 능력 더 키워줘"→4방향 전부 선택): ①컴파일 에러 정밀 파싱(unity_run이 file(줄,칸)CSxxxx로) ②unity_new_script 스캐폴딩(mono·scriptable·editor·test+asmdef·plain) ③unity_find 실시간 코드검색 ④EditMode 테스트 자동화. 개발 루프=find→new_script→edit_document→unity_run. 실물 종합 E2E 통과(실 OnlyUpRat, 스캐폴드컴파일·CS1525파싱·검색·테스트통과1, 증분 6~21초). 상세 [[project-my-agent]] 세션57.
- ✅**블렌더·유니티 보완 3차**(같은 세션, "보완/향상할 부분?"→4방향 전부 선택): ①unity_status(에디터 안닫고 Editor.log 컴파일에러 확인) ②blender_batch(폴더 일괄 info/convert/prep_unity/수술) ③unity_build(배치모드 APK/AAB, config.json unity.build_methods) ④blender prep_unity(유니티용 원클릭 apply+원점+cleanup+FBX+자동검증). 실물 E2E 통과. 상세 [[project-my-agent]] 세션57 5부.
- ✅**블렌더·유니티 심화(세션57 7부, "둘 다 만들어줘")**: 테마1 감사/QA(**blender_3d check** 반입 린트·**unity_audit** 깨진참조/큰텍스처 스캔) + 테마2 효율(**blender_3d chain** 여러작업 한부팅·**unity_run 잠금 프리플라이트**). 실 E2E 통과(unity_audit가 그림4.png 11.7MB 등 실제 포착). 원칙: 눈 없는 루시=기계적·숫자·반복, 시각·조형=클로드.
- **미래 확장 후보**(사용자 요청 시): ①code_run/unity_run 웹·배경 지원 ②venv 격리 실행환경 ③파라메트릭/그레이박스 생성(테마3=눈 없이 만들 수 있는 것) ④bpy/유니티 API 치트시트 knowledge화(테마5=환각↓) ⑤루시 자기 코드 리뷰 ⑥게임 asmdef 분리해 게임코드 테스트 ⑦PlayMode 실사용 ⑧전체 APK빌드 실검증 ⑨텍스처 임포트설정 자동 하향(감사 후속).

## ✅자기 계측(관측성) 완료 (세션57, 2026-07-16)
- **문제였던 것**: status.record(label)은 **성공 카운트만** 셌음 — 두뇌별 응답시간·실패율·폴백 빈도를 아무도 기록 안 해 "느려/이상해"에 근거가 없었음.
- **한 일**: `memory/metrics.json` 신설(usage.json과 같은 14일 보관·원자적 저장). agent.call_model 성공 시 `status.record_call(label, 걸린초, 순위)` — 순위>0이면 폴백 집계. 실패 시 `status.record_fail(label, 종류)` — 종류는 quota·too_big·too_long·authkey·http·conn·noanswer(fail_kind 변수 한 곳에서 기록). `/상태`에 **[계측]** 섹션: 두뇌별 평균·최대 응답시간·폴백 횟수·실패 내역{종류별}+오늘 폴백 총합. 전부 조용(계측이 대화를 죽이면 안 됨). 매뉴얼 /상태 항목 갱신함.
- **실물 E2E 통과**: record_call/record_fail→metrics.json 되읽어 값 검증(ok·fb·s_sum·s_max·fail 전부 일치)+status.report의 [계측] 렌더 확인.
- 나머지 후보(세션56 제시): ②ntfy 폰 푸시(도달성, **다음 1순위**) ③웨이크워드 "루시야" ④견고성(Ollama 의존·조용한 실패). 향후 계측 확장: 만성 느린 두뇌 자동 후순위·조용한 고장 경보.

## 보류(사용자 입력 대기)
- ✅**대중교통 완료(세션53 후반)** — transit.py+get_bus(도구 35종), 인천·경기(부천)·서울 3어댑터, E2E 전부 통과. 함정·프로브 기법은 [[project-my-agent]] 세션53 후반 절. ✅집 앞 버스도 설정 완료(532=구월초등학교·536=남동소방서, 동암남부역 방면, config transit.home — "집 앞 버스 언제 와?" E2E 통과). ✅Cerebras 8192 초과도 같은 세션에 수리(COMPACT_SPECS — 상세는 [[project-my-agent]] 세션53 후반. ⭐새 도구 설명 쓸 때 핵심 조향은 첫 160자 안에).
- **웨이크워드 "루시야"** — ⑦ 연속 음성 실사용 검증 뒤. openWakeWord(한국어 커스텀 학습) vs Porcupine(무료 키 1개). 상시 마이크=프라이버시·CPU 부담 고지 후 시작.
- (기각) IoT — 스마트홈 기기 없음(2026-07-13). 기기 생기면 재론.

## ✅세션56 4부: '어중간한 기능' 보완 4건 완료 (2026-07-15, 사용자 4건 전부 선택)
한글 HWPX 읽기 · 엑셀 셀 치환(edit_document) · 웹 마크다운 렌더 · 대화 의미 검색(histsearch.py). 상세·함정은 [[project-my-agent]] 세션56 4부. 이때 같은 세션에 자동 귀 찾기(마이크 갈아타기)·두뇌 재배치(속도, Gemini 뒤로)도 완료(3부).

**Why:** 이 파일은 '착수 대기 + 사용자 확인 대기 + 다음 후보' 목록. 지난 승인분은 전부 구현됨(세션52~61).
**How to apply:** **"루시 작업하자"가 나오면 맨 위 🔜'다음에 할 것' 절부터.** 세션63 승인 3건(프로젝트 건강 감시·계측 자동 후순위·눈 신뢰도)은 **세션64에 전부 완료**됐다 — 남은 것은 승인된 대기가 아니라 **후보**이므로 착수 전에 무엇부터 할지 물을 것. 순서 권고: A(관찰 뒤 조정, 실제로 써 봐야 판단됨) → C7(실전 한 바퀴) → B(웨이크워드). 그 뒤 남는 후보는 웨이크워드 "루시야"·지하철 정식 키(선택). ⛔폰 원격 계열(ntfy·LTE·Tailscale HTTPS·WoL)·손 강화·adb는 다시 제안하지 말 것. 관련: [[project-my-agent]].

## ✅세션59: 블렌더 능력 확장 완료 (2026-07-16, 사용자 "블렌더 작업 능력 더 키워줘"→4방향 전부 선택)
anim_preview(움직임 GIF)·bone_template(표준 뼈대 배치)·tex_resize(텍스처 다이어트)·lod(LOD FBX)·build(그레이박스 조형)·assemble(씬 조립) — blender_3d 6동작 추가, E2E 9/9+눈검수. 상세·함정은 [[project-my-agent]] 세션59. 남은 후보 목록은 위와 동일(ntfy 폰 푸시 등).

## ✅세션59 2부: 블렌더 21→31동작 (같은 날, "30동작으로 더 늘릴 항목 없나"→후보 10종 전부 선택)
mirror·array·scatter·text3d(조형·배치) + collider·materials·split(유니티 실전) + anim_edit·weight_transfer(애니·리깅) + compare(QA). E2E 20/20+눈검수(radial 대형 겹침 버그를 눈검수로 잡음, 블렌더 5.0 Action.fcurves 제거 대응). 상세는 [[project-my-agent]] 세션59 2부. (~~블렌더 포화~~ → 세션61에 새 10종 승인됨, 아래 절 참조.)

## ✅블렌더 10종 완료 (세션61 승인 → 세션62 2026-07-17 저녁 구현, 실물 E2E 10/10+도구레벨 9종+눈검수)
클로드가 제안→사용자 "10개 메모리에 저장해줘, 오늘 저녁에 작업할게" → "루시 작업하자"로 재질문 없이 착수(설계대로 작동). 구현 상세·함정 3건(PBR 접미사 매칭·Cycles 후 EEVEE 크래시·FBX 라이트 몽키패치)은 [[project-my-agent]] 세션62 절. 블렌더 31→41동작.
**다음 후보: ①웨이크워드 "루시야" ②지하철 정식 키.** (~~ntfy 폰 푸시 1순위~~ → 2026-07-18 폰 원격 중단 결정으로 제외, 맨 위 절.) ✅차점 3종(pose_apply·physics_bake·purge)은 **세션63(2026-07-18) 블렌더 10종에 포함 완료** — bevel·solidify·shade·lightmap_uv·rename·normals·align까지 41→51동작, E2E 14/14. 상세·함정(헤드리스 bake_to_keyframes 죽음→데이터 API 직접 굽기, purge=fake user가 진짜 찌꺼기)은 [[project-my-agent]] 세션63 절. ✅**판단력 로드맵 3층 전부 완성(세션63 2~5부)**: 1층 👁눈검수(블렌더 수술+unity_shot)·2층 📏예산 대조(블렌더·유니티 기준표)·3층 🎓작품 지적 학습(is_craft_complaint→craft 교훈→도구명 기준선 0.45 회상). 이제 남은 후보는 웨이크워드·지하철 정식 키뿐.

**파이프라인 (효과 큰 순):**
1. **anim_merge** — 애니 FBX 여러 개(Mixamo류, 같은 뼈대)를 캐릭터 하나의 FBX로 합본(유니티 다중 클립). 액션 이름=파일명, 내보내기 후 재임포트해 액션 수·이름 자가검증. ⚠️블렌더5.0 layered actions는 anim_edit 때 만든 channelbags 다리 재사용. 본 이름 불일치면 정직 거절.
2. **bake** — 하이폴리→로우폴리 노멀/AO 베이킹(Cycles 헤드리스, unity_export의 bake_decal 러너 재사용). decimate·lod와 세트로 '폴리↓ 디테일 보존'. 출력 PNG는 ASCII 명명+외부 파일 방식.
3. **repair** — check(진단)의 수리 짝: 비매니폴드·n-gon·구멍을 fill_holes→dissolve_degenerate→(심하면 voxel remesh) 단계 수리, 수리 후 check 재실행해 전/후 비교 자동 출력.
4. **uv_atlas** — 여러 오브젝트 재질·텍스처를 아틀라스 한 장으로(드로우콜↓). 1차=재질 병합+UV 재패킹, 2차=Cycles 베이크 합성(무거우면 1차만 먼저).

**조형 확장:**
5. **boolean** — ops=[{target,tool,mode:union/difference/intersect}] 스펙 CSG(exact solver). build 그레이박스에 창문 뚫기·구멍 내기. 적용 후 cleanup 권장 안내.
6. **curve_path** — points=[[x,y,z]…] 경로로 pipe(bevel_depth)·ribbon 생성 or 오브젝트를 커브 따라 배열(Array+Curve 모디파이어 적용). array(직선·원형)의 자유곡선판.
7. **sockets** — items=[{name,pos,rot}]로 부착점 Empty 심기(부모 지정 가능, 이름 ASCII 강제). FBX에 포함→유니티 장착 포인트(손·총구). 구현 쉬움.

**보는 도구(사용자 눈 보조):**
8. **contact_sheet** — 폴더의 3D 파일 전부 1컷씩 렌더→PIL 몽타주 한 장(파일명 라벨). "받은 에셋 20개 뭐가 뭐야"용. blender_batch 확장.
9. **beauty_render** — 기획서용 렌더: 3점 조명+바닥 그림자(shadow catcher)+투명 배경 PNG, 해상도 지정. 기존 render(점검용 4방향)와 별개.

**재질:**
10. **material_pbr** — 텍스처 폴더에서 albedo/normal/rough/metal/ao를 파일명 규칙으로 자동 매칭→Principled 연결(normal·rough는 Non-Color), ASCII 개명, unity_export 연계. '갈색 덩어리' 사고를 반입 단계부터 예방.

차점(이번 미포함, 다음 제안 때 후보): pose_apply(포즈 JSON→키프레임, 클로드 설계·루시 적용+GIF 분업) · physics_bake(천·강체 시뮬 굽기) · purge(미사용 데이터 청소).


## (2026-07-18 색인 압축 때 이관한 원문)

- [루시 기능 백로그](todo_lucy_feature_backlog.md) — ✅**블렌더 또 10종(세션63, 2026-07-18): 41→51동작** — bevel·solidify·shade·lightmap_uv·rename(본 불가침)·normals(signed volume 진단, s61 노하우 도구화)·align·purge(⭐실파일 찌꺼기=fake user 데이터, 기본 보고만·include_fake로 삭제)·pose_apply(포즈 JSON→키프레임, 설계=클로드 분업)·physics_bake(rigid 낙하 굽기·cloth 드리움, ⭐헤드리스 bake_to_keyframes 죽음→데이터 API 직접 굽기), E2E 14/14+눈검수 4장. ✅**세션63 2부: 👁 수술 자가검수=판단력 1층(비전 폐루프)** — 수술·build·assemble 미리보기를 눈 두뇌(Gemini)에 보여 "정상/문제:" 1차 판정을 결과 줄에 덧붙임(명백한 실패만=공중부양·뭉침·소실·뒤집힘, 미적 판단은 여전히 사람). ⭐오탐 2건 실측 수리: 천 자락 늘어짐=정상 명시 문구·**배치류(align·array·scatter) 미리보기는 부감으로**(낮은 앵글=원근 겹침 오판). 실패해도 수술 결과 불가침, 끄기=blender.eye_check=false. ✅**세션63 3부: 📏 예산 자동 대조=판단력 2층** — knowledge/blender_모바일_기준표.md(소품 5,000tri·캐릭터 30,000tri·재질 8·본 75, config blender.budgets로 변경)+info(total_tris 정확 계수·뼈대=캐릭터 기준 자동 판별)·check·폴리 수술 6종에 📏 경고. ⭐check의 tris=삼각형 '면 개수'(쿼드=0)라 예산 대조 불가→면×2 추정(E2E 실측). **판단력 로드맵 남은 것: 3층=블렌더 지적→lessons 연동.** ✅**블렌더 10종 완료(세션62, 2026-07-17 저녁)**: anim_merge 애니합본(NLA 스트립 방식)·bake 노멀/AO·repair 수리·uv_atlas·boolean·curve_path·sockets 부착점·contact_sheet 폴더시트·beauty_render·material_pbr — 블렌더 31→41동작, E2E 러너 10/10+도구 9종+눈검수. ⭐함정 3(PBR 접미사 우선 매칭·**Cycles 베이크 후 같은 부팅 EEVEE=크래시**·FBX 라이트 몽키패치 상시화)은 project_my_agent 세션62 절. ⛔**폰 원격 개발 중단(2026-07-18 사용자 결정: "PC 안 켜면 못 쓰는 디메리트가 너무 커" — ntfy 푸시·LTE 검증·Tailscale HTTPS·WoL 다시 제안 금지, 기존 웹·Tailscale은 유지만)**. **다음 후보: 웨이크워드·지하철 정식 키.** 완료 이력: 세션52 7건 + 세션54 10건 + 자기제안 3건 + 세션55 3건+문서수정+영상+자막 + 세션56 4건(doctor 9종·색인 신선도·PPT 슬라이드 추가·웹 긴작업 /poll). 루시 제안서 나머지는 기각/보류. Tailscale 외부접속 완료(PC=100.74.78.102). ⭐**도구 설명의 핵심 조향은 '160자 안에서 끝나는 첫 문장(들)' 안에**(작은 두뇌엔 COMPACT_SPECS — 세션56에 edit_document서 3번째 재발). ✅연속음성 실사용 완료(세션56: 목소리는 Galaxy Buds3인데 기본 장치=Focusrite가 원인 → voice.device="Buds"(이름 매칭, 번호 바뀜·끊김 흡수)+**문턱 25·noise_mult 2.5**(버즈 AGC 출렁임 — 소음 측정은 평균 아닌 최솟값)+**자동 귀 찾기**(첫 침묵 시 전 장치 동시 청음→갈아타기→config 저장, 새 헤드셋도 무설정 — 스테레오 믹스 제외 필수), 육성 받아쓰기 통과 — 웨이크워드 선행 조건 해제. ⚠️`음성` 푸시투토크(MCI)는 윈도우 기본 장치를 따름). ✅**답변 속도 근본 수리(세션56 9부)**: "9/10 느림"의 뿌리=Cerebras ctx8192인데 **도구 41개 명세만 4712토큰=58% 소진** → 조금만 길어도 초과→느린 두뇌 추락(3부 두뇌 재배치는 증상완화였음). 해법=**tool subsetting**(작은 두뇌엔 핵심17종+질문 키워드 관련만, `specs_for(entry, messages)`. 안전4중: 핵심항상·이미 부른도구 유지·큰두뇌 전부·system 스캔제외). 실측 오버헤드 58%→24~30%, **평균 2543토큰/턴 절감**(여유 2배). 실물 E2E 통과(핵심 get_weather·게이트 blender_3d 둘 다, 폴백 경로 포함). (~~확인 대기 LTE·Tailscale HTTPS~~=폰 원격 중단으로 소멸). ✅**자기 계측(관측성) 완료(세션57)** — memory/metrics.json 신설, call_model 성공 시 record_call(초·순위, 순위>0=폴백)·실패 시 record_fail(종류), /상태 [계측] 섹션(두뇌별 평균/최대 응답시간·폴백·실패내역), 실물 E2E 통과. 다음 후보=웨이크워드 "루시야"·지하철 정식 키(선택, ntfy는 폰 원격 중단으로 제외). 계측 확장(느린 두뇌 자동 후순위·조용한 고장 경보)은 이 토대 위에. ✅**코딩 작업실 완료(세션57)** — 사용자 "프로그래밍 기능 강화" 신규 요청→방향 4개 중 '짓기·고치기' 선택→coding.py+도구 6종(workspace/ 격리, 편집-실행-디버그 루프, 실행만 프로젝트당 세션1회 확인), 실물 E2E 통과. IoT 기각. PC 꺼지면 접속 불가.
