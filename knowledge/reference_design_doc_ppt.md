---
name: reference-design-doc-ppt
description: "게임 기획서 PPT — Desktop\\SaladFarm_게임기획서.pptx (세션51 Ver 1.5·24슬라이드, 외부 리뷰 9건 반영). python-pptx로 rPr deepcopy 편집(한글폰트 보존)+COM export 검수."
metadata: 
  node_type: memory
  type: reference
  originSessionId: e22249c8-f63f-4f50-bbb7-336a86f9a495
---

# 게임 기획서 PPT (2026-07-04 세션27)

## 2026-07-08 세션32 전면 최신화 (Ver 1.0→1.1, 백업 .bak_s32)
- s6 생명체: 총 41종, 티어×식단 표 재계산(베이직 4/4/3/4=15, 레어 2/1/2/3=8, 프리미엄 4/3/3/4=14, 시크릿 4), "최소 4종" 문구 폐기, 아트 현황(신규 진화 도트 21종), 도감 3칸 진행도.
- s9 경제: 생산율에 성장 ×1.5 추가, 다이아 획득에 '생물 보내기 +1' 추가. s13 수면 "전 종(단계 포함)". s14 인벤에 보내기 작별 선물 규칙. s17 APK 97.6MB. s18 41종. s12 불꽃 꼬리 사자. s1 Ver 1.1.
- 시작 재화는 코드 확인 결과 골드 500/다이아 10 유지(release-prep 메모의 100은 오기).
- 편집 방법: run 단위 텍스트 치환(서식 보존, 여러 run에 걸치면 문단 합침 폴백) — ppt_update.py 패턴. COM Export 검수 완료.

**산출물**: `C:\Users\user\Desktop\SaladFarm_게임기획서.pptx` — 16:9, 18슬라이드, 게임 팔레트(크림/세이지/갈색)+맑은 고딕, 인게임 스크린샷 3장 포함.

구성: 표지/개요/디자인 철학(숙제 금지 등 4기둥)/코어 루프/AI 분석(수치)/생명체 65종 매트릭스/부화/성장·진화(경험치 표)/경제(재화 순환·회수 계산)/상점/날씨 표/시크릿 생태계/환경 연출/서브 콘텐츠(출석 표)/온보딩/아트·UI/기술 스펙/현황·로드맵.

**생성**: python-pptx(설치됨). 스크립트 `gen_ppt.py`는 세션 스크래치패드라 소멸됨 — 수정 필요 시 PPT를 직접 편집하거나 재작성. 한글 폰트는 run의 rPr에 a:ea typeface 주입 필요(라틴만 설정하면 한글이 기본폰트로 빠짐). 레이아웃 검수는 PowerPoint COM(`Slides.Item(n).Export`)으로 PNG 뽑아 확인하는 패턴이 유효했음.

수치 출처: 코드 상수 직접 확인(BaseFarmCapacity 10/Max 30/확장 다이아10, 알 100·300G, 배경 50G, 출석 100G/1D/200G/2D/300G/3D/프리미엄알, 성장아이템 150G). 이후 밸런스 변경 시 PPT도 갱신 필요.

## 2026-07-05 세션28 갱신 (반영 완료)
꾸미기/사진 변경을 3개 슬라이드에 반영. **편집 방식**: python-pptx로 대상 run의 rPr을 deepcopy해 재적용(한글 a:ea 폰트 보존) → 한 문단 내 여러 줄은 a:br로 구성. **검수**: PowerPoint COM Export로 슬라이드 PNG 뽑아 오버플로/폰트 확인. 편집 전 `Desktop\SaladFarm_게임기획서_백업_0705.pptx`로 원본 백업.
- **슬라이드10(상점) 꾸미기 카드**: 3종→**12종·새 가격**(나무200/호수300/꽃5색 각100/버섯150/벤치300/랜턴300/풍차500/우물800G, 8×12 그리드). 한 줄 1아이템으로 재구성(좁은 카드 줄바꿈 회피).
- **슬라이드13(환경 연출) 하단 노트**: "꾸미기 일부는 살아 움직인다 — 밤 랜턴·버섯 점등, 풍차 날씨 회전, 벤치 생물 앉기" 한 줄 추가.
- **슬라이드18(로드맵)**: "스크린샷 공유"(출시후) → "사진 공유 시트 (촬영·갤러리 저장·미리보기 완료 · NativeShare만 남음)"로 갱신. [[feature-farm-decorate]] [[feature-farm-photo]]

## 세션35 갱신 (Ver 1.2, 2026-07-09)
- **18 → 20슬라이드**: `생명체 로스터`(s7, 32종 전체 이름표) + `생물 연출 & 아트 파이프라인`(s18) 신설.
- 신규 슬라이드는 **그림 없는 슬라이드(구 s6·s17)를 XML 통째로 deepcopy**해서 만든다. 그림 있는 슬라이드를 복제하면 `r:embed` 관계가 깨진다. 삽입 후 `_sldIdLst` 재배치 + 페이지 번호 텍스트박스(left>11.0M, top>6.0M) 전부 재부여.
- 내용: 41→**32종**(랩터 추가 삭제), 시크릿 개명(레온·삿싱), 시크릿 1.5배·보내기 불가, 세계수 요정 정지형, 성장(5레벨) 단계 명시, 출석 시계 되감기 무효·관리자 모드 상용 배제.
- ⚠️ **run 치환 함정**: 대상 문자열이 여러 런에 걸치면 첫 런에 합쳐져 **굵은 라벨 서식이 날아간다**(s17에서 발생). 합쳐진 뒤엔 `r[0]='•  ' / r[1]=라벨 / r[2]=본문`으로 3분할 복원.
- ⚠️ **문단 전면 재작성 금지**: 하위 들여쓰기 문단(`   -  …`)이 불릿으로 바뀌고 박스 높이를 넘겨 잘린다. 백업본에서 도형 XML을 되돌린 뒤 문단을 **하나씩만** 복제하는 게 안전.
- 검수는 PowerPoint COM `Slides(i).Export(png)` 후 이미지 확인. 백업 `.bak_s35`, `.bak_s35_pre_expand`.

## 세션36 갱신 (Ver 1.3, 21슬라이드, 2026-07-09)
- **s21 `설계 노트 — 정직성은 검증하지 않는다` 신설**. 갤러리 가져오기 유지 결정의 근거를 문서화. 내용은 [[project-no-authenticity-verification]].
- 신규 슬라이드를 **처음부터 그리는 방식**도 안전함(복제 불필요): `slide_layouts[6]`(Blank) + 배경 사각형부터 직접 배치. 디자인 토큰 = 배경 `F8F1E4` / 카드 `FDF8EE` + 테두리 `E2D5BE` 1pt / 잉크 `5A4A3A` / 보조텍스트 `8A7864` / 페이지번호 `E2D5BE` / 밴드 `EFE4CE`, 액센트 초록 `6F9E5A`·주황 `E29D59`·분홍 `BF80A6`·하늘 `70C6E2`. 제목 30pt bold(0.82,0.34), 영문부제 14pt(0.85,0.98), 페이지번호 12pt bold 우측(12.35,6.98), 좌측 액센트바 0.14×0.62(0.55,0.42). 카드=상단 컬러바 h0.16을 같은 좌표에 겹쳐 그림.
- ⚠️ 새 도형엔 `shape.shadow.inherit = False` 필수(안 하면 기본 그림자가 붙어 톤이 깨짐). 한글은 여전히 rPr에 `a:ea`/`a:cs` typeface 주입 필요.
- ⚠️ 스크래치 스크립트를 `inspect.py`로 만들면 표준 `inspect` 모듈을 가려 lxml import가 순환참조로 실패한다. 다른 이름을 쓸 것.
- 백업 `.bak_s36`. 표지 Ver 1.2 → **Ver 1.3**.

## 세션37 갱신 (Ver 1.4, 22슬라이드, 2026-07-09, 백업 `.bak_s37b`)
- **s6 `식단 기록 & 갤러리` 신설**(s5를 통째로 deepcopy — 그림이 없어 안전). 좌측 불릿 7개 + 우측 '사진 저장 스펙' 표(원본 720px q82 / 썸네일 200px q75 / ≈17KB / 1년 ≈18MB / 앱 내부). 삽입 후 페이지 번호 전 슬라이드 재부여.
- s4 코어 루프 ①에 '수동 입력' 추가. s5 AI 슬라이드는 **v5로 갱신**(치킨 근접샷 홀드아웃 34→97%, 표 제목 v4→v5) + **신뢰도 3대역 정정**(기존 "0.55 미만이면 재촬영"은 오기. 실제는 0.55↑ 확정 / 0.30~0.55 균형 / 0.30↓ 재촬영) + 수동 입력 불릿.
- s16 서브 콘텐츠·s20 기술 스펙(사진 저장 · 빌드 시 개발자 모드 선택 · GPGS 플러그인)·s21 로드맵 완료 항목·s22 설계 노트 결론(사진은 기기 밖으로 안 나감) 반영.
- ⚠️ **좌측 불릿 박스(W 5760720, H 4937760)는 약 12~13줄이 한계.** 불릿 9개(17줄)면 아래가 잘린다. 한 불릿을 1~2줄로 눌러 쓸 것.
- ⚠️ 로드맵/완료 항목 텍스트박스는 `r[0]='✔  '`(초록 bold) + `r[1]=본문`(잉크) **두 런 구조**다. `set_text_keep_style`로 한 줄만 넣으면 첫 런 서식이 전체에 먹어 **본문까지 초록**이 된다. 원본을 deepcopy해 `runs[1]`의 텍스트만 갈아끼울 것.

## 세션51 갱신 (Ver 1.5, 24슬라이드, 2026-07-13, 백업 `.bak_s51`)
- **외부 리뷰 보고서**(Desktop\SaladFarm_기획서리뷰보고서.docx, 구멍 9건) 대조 반영 — **수용 7·기각 2**. 기각: #4 출석 프리미엄알(이미 s16 표에 7일차 명시 = 리뷰어 오독), #7 친구농장 상세(후순위가 설계 — s22 로드맵에 "진위 검증 재검토 선행" 한 줄만).
- **s17 `도전과제 전표 — 18종` 신설**(s13 복제, 3열 표 2개 좌우 배치): AchievementBook.cs 실측 — 조건·보상 18종 전부, 골드 2,650+다이아 35, 도감 박사=24종(전체×3/4).
- **s23 `수익화 & 리텐션 검토` 신설**(s16 복제): 다이아 판매 불성립 논증(싱크가 확장 40뿐), 광고=보상형 배율만·IAP=코스메틱 전용, D1 40/D7 20/D30 8% **가설 목표**(내가 채운 수치임을 슬라이드에도 '가설'로 명기).
- 소수정: s9 균형 식단 이득 구체화(도감→다이아 32·날씨 선택권), s13 "햇살(+무지개)"→햇살+특수효과 "가끔 옅은 무지개 연출"(코드상 무지개는 별도 날씨 아님), s14 특징란 "패널티 없음"/"유일 패널티" 명시, s16 도전과제 요약+17쪽 참조, s21(기술) 어뷰징 4겹 구체화(음수 경과 0 처리 등), 페이지번호 전체 재부여.
- ⚠️ 표 재구성은 기존 tr/tc를 deepcopy 템플릿으로 gridCol·tr 전체 재작성(rebuild_table 패턴, 지브라 유지). tr h는 최소값일 뿐 렌더는 폰트+마진으로 커진다 — **표 아래 도형 y는 COM PNG로 실측 후 배치**(s17에서 겹침 1회 수정).
- ⚠️ run 치환 시 문단이 여러 런이면 중복 텍스트 발생(s22 소셜 줄에서 재발) — runs[0]에 전문 넣고 나머지 런 제거.
